# Handoff Prompt - CivicCast 3.3 To 4.0 Coder Effort

> **Superseded source-control rule:** This 2026-07-02 handoff is retained for
> product history, specifications, and lessons learned. Its case-by-case
> commit/push restriction was explicitly retired by Scott Converse. Ordinary
> scoped commits and pushes are authorized after the current rigor gates and
> normal review/CI path. Force-pushes, gate bypasses, merges, tags, release
> signing, publication, and shipment remain owner-gated. For native-Windows
> recovery roles and sequencing, use
> `docs/process/CODEX-NATIVE-BETA-HANDOFF-2026-07-24.md` and
> `.agent-runs/native-windows/specs/spec-native-beta-recovery.md`.

Date written: 2026-07-02

Use this as the handoff prompt for a new Codex session or another AI taking over CivicCast development.

## Copy/Paste Prompt For The New Session

You are taking over the CivicCast coder effort from a previous Codex session. Your job is to continue from the exact local repo state, preserve the released 3.2 beta, and implement the 3.3-to-4.0 roadmap locally until the product is complete under the saved spec.

Start by verifying the current state. Do not assume this handoff is still perfectly current; run the repo identity checks below first.

### 1. Repo And Workspace

Use this repo:

```powershell
Set-Location C:\CivicCastTester\v3-beta-release-prep
git status --short --branch
git remote -v
git rev-parse HEAD
git log -1 --oneline --decorate
git tag --points-at HEAD
```

Expected state after the Stage 1 checkpoint refresh:

- Repo path: `C:\CivicCastTester\v3-beta-release-prep`
- Remote: `https://github.com/scottconverse/civiccast.git`
- Branch: `local/3.3-stage1-install-first-run-gate`
- Current Stage 1 checkpoint HEAD: use `git rev-parse HEAD`, then confirm the same
  value appears in `artifacts/stage-reports/3.3-stage1-final/stage-report.json`.
- Full stack evidence: use the `full-stack` check path in
  `artifacts/stage-reports/3.3-stage1-final/stage-report.json`.
- Release artifact manifest: `artifacts/release/v3.3.0-stage1/civiccast-3.3.0-release-artifacts-manifest.json`
- Clean Windows proof: `artifacts/clean-windows/3.3-stage1-final/clean-windows-install.json`
- First-run attestation: `artifacts/first-run/3.3-stage1-final/first-run-attestation.json`
- Clean Windows screenshot: `artifacts/clean-windows/3.3-stage1-final/vbox-stage1-first-run-visible.png`
- Stage 1 report: `artifacts/stage-reports/3.3-stage1-final/stage-report.json`
- GauntletGate report: `artifacts/gauntletgate/3.3-stage1-final/00-gate-report.md`

Treat the listed release/clean-Windows/first-run artifacts and the `full-stack`
path recorded in the final stage report as the canonical Stage 1 evidence set.
Do not use an older stage report or GauntletGate report as source of truth; those
reports are valid only after they are regenerated and stamped to the current
Stage 1 closeout commit.

The original 3.2 beta handoff baseline, preserved for release continuity, was:

```text
Branch: main
HEAD: ab9c115a1b82d4cc2a628a2b4d37e85b08d78948
HEAD decoration: HEAD -> main, origin/main, origin/HEAD, tag: v3.2.0-beta1
Last commit: Merge pull request #183 from scottconverse/local/3.2-lpm-contract-lab
```

`v3.2.0-beta1` pointed at that original baseline. Do not conflate the published 3.2 beta with the active 3.3 Stage 1 work branch.

Do not use OneDrive as the repo/workspace root. The correct working repo is `C:\CivicCastTester\v3-beta-release-prep`.

### 2. Current Published Beta Baseline

The current public beta baseline is already published:

- GitHub release: `https://github.com/scottconverse/civiccast/releases/tag/v3.2.0-beta1`
- Release name: `CivicCast v3.2.0-beta1`
- Release state at handoff: published, prerelease, not draft
- Asset count at handoff: 26
- Windows installer asset: `civiccast-3.2.0-beta1-windows-setup.exe`
- Windows installer URL: `https://github.com/scottconverse/civiccast/releases/download/v3.2.0-beta1/civiccast-3.2.0-beta1-windows-setup.exe`
- Windows installer size from GitHub release metadata: `248282495` bytes
- Windows installer SHA-256 recorded in release artifacts: `29167d2f1828b8ca0202e5a8c1c2fa5a8ed06af8f1cc8ebfa3e79d365319f662`
- Release docs: `docs/releases/v3.2.0-beta1.md`
- Verification docs: `docs/releases/v3.2.0-beta1-verification.md`
- Tester handoff: `docs/tester/lpm-beta-test-handoff.md`

Do not disturb the released 3.2 beta unless the user explicitly asks. For 3.3-to-4.0 work, create a local work branch when proceeding and keep commits local unless the user explicitly authorizes a push, merge, tag, or release.

### 3. Most Important User Rules

The user wants the 3.3-to-4.0 work finished, not repeatedly re-scoped. The saved spec is the source of truth.

Rules to preserve:

- If something can be implemented, simulated, documented, packaged, tested, or verified locally in software, it belongs in the 3.3-to-4.0 plan.
- The only exclusions are validations that require a third-party physical facility, external operators, cable/headend access, app-store approval, or accounts/systems outside local control.
- Do not quietly defer software-doable work.
- Do not add caveats as a substitute for fixing required work.
- Required skipped checks count as failures unless the same capability is proven through an equivalent enabled substrate in the same report.
- Do not call a stage or release done with open blocker, critical, major, minor, or nit findings from the relevant final gate.
- Use GauntletGate exactly as instructed when it is requested. Read `C:\Users\scott\.codex\skills\gauntletgate\SKILL.md` before running it.
- If AntiStall is invoked or active, read `C:\Users\scott\.codex\skills\antistall-codex\SKILL.md` and obey it. Even when it is not active, do not stop with status-only drift when the next implementation step is clear.
- The user has authorized full local/admin/computer-use access for trusted development tasks, but still pause before destructive, security-sensitive, credential-sensitive, or hard-to-reverse actions.
- Do not push, merge, tag, publish, or release unless the user explicitly asks.
- Do not handhold tests through failures to make them pass. Preserve failures, fix code/product/test harness issues, and rerun.
- Avoid using "future outside validation" language as an excuse for not building software. Build the local software, harnesses, docs, and tests now.

### 4. Required First Reads

Before coding, read these files:

```powershell
Get-Content .\CLAUDE.md
Get-Content .\docs\spec\3.3-to-4.0-sprint-list-and-implementation-plan.md
Get-Content .\docs\spec\3.2-lpm-livestreaming-contract-lab.md
Get-Content .\tools\virtual-media-studio\README.md
Get-Content .\docs\releases\v3.2.0-beta1-verification.md
Get-Content "C:\Users\scott\Documents\Codex\2026-07-01\i-need-a-deep-dive-into\outputs\civiccast-broadcast-market-expansion-deep-dive-2026-07-01.md"
```

Read the new roadmap/spec first after `CLAUDE.md`. It is the durable worklist:

`C:\CivicCastTester\v3-beta-release-prep\docs\spec\3.3-to-4.0-sprint-list-and-implementation-plan.md`

That spec contains:

- The complete 31-item 3.3-to-4.0 scope.
- The seven-stage implementation plan.
- The important installer sequencing correction:
  - Stage 1 builds the installer substrate and clean-machine gate.
  - Every later stage updates the installer as new components land.
  - Stage 7 builds and verifies the final 4.0 release installer.

### 5. Current Local Software/Lab Environment Observed At Handoff

Verify this again before relying on it.

Running media/lab-related processes observed at handoff included:

- `obs64` running.
- `vMix64` running.
- `vMixNDIHelper` running.
- `vMixService` present.
- Several `node` and `node_repl` processes.
- WSL was active.

WSL distro state observed at handoff:

```text
Ubuntu                         Running  Version 2
Ubuntu-24.04                   Running  Version 2
CivicCast-Ubuntu-24.04         Running  Version 2
docker-desktop                 Running  Version 2
```

VirtualBox VMs observed at handoff:

```text
civiccast-v13-cleanwin
civiccast-v200-proof-cleanwin
civiccast-cleanwin-v2
civiccast-v3-r6-cleanwin
```

Do not assume any VM is clean without resetting or inspecting it. Use the VM scripts and document the state before clean-install claims.

### 6. Useful Existing Paths And Scripts

Virtual media lab:

```text
tools/virtual-media-studio/
tools/virtual-media-studio/civiccast-vstudio.py
tools/virtual-media-studio/vstudio/
```

Important scripts discovered at handoff:

```text
scripts/bootstrap_virtualbox_clean_windows_vm.ps1
scripts/prepare_cleanroom_environment.ps1
scripts/prepare_virtualbox_civiccast_workspace.ps1
scripts/run_vm_cleanroom_release.py
scripts/run_clean_windows_install_proof.py
scripts/run_local_3_2_lpm_contract_lab_ci.ps1
scripts/run_lpm_contract_lab.py
scripts/run_lpm_contract_lab_wall_clock_soak.py
scripts/run_stage_gate.ps1
scripts/run_full_test_stack.ps1
scripts/roadmap_status.py
scripts/build_release_artifacts.py
scripts/collect_source_state.py
scripts/generate-openapi-artifacts.py
```

Common artifact roots:

```text
artifacts/local-ci/
artifacts/gauntletgate/
artifacts/lpm-contract-lab/
artifacts/vstudio/
artifacts/wall-clock-soak/
artifacts/release/
```

### 7. The 31 Items To Complete By 4.0

The saved spec has the authoritative full text. The item headings are:

1. Installer, Clean Install, Repair, Update, And Uninstall
2. First-Run And Operator Setup
3. Every-Screen UX Walkthrough Closure
4. Three-Channel Station Workflow
5. Media Library And Playout Hardening
6. Recording And Ingest Hardening
7. Control-Room UI
8. vMix Integration
9. OBS Integration
10. ATEM Integration
11. PTZ / VISCA / AIDA Integration
12. NDI Integration
13. DeckLink / Desktop Video / Capture-Card Paths
14. Audio Mixer And Audio Device Layer
15. Routers, Encoders, And Destination Profiles
16. Virtual Media Studio Lab
17. LPM-Style Profile Packs
18. Migration And Import Tools
19. Traffic, Ads, Underwriting, And SCTE-35
20. Redundancy And Disaster Recovery
21. Accessibility, Captions, Translation, And Compliance Packets
22. Agenda, Minutes, Records, And Clerk Integrations
23. Producer, Volunteer, And Equipment Operations
24. Education / Campus Package
25. Archive, MAM, And Digitization
26. Proof, Reports, Support Bundles, And Acceptance Packets
27. Security, Secrets, Roles, And Audit
28. Performance, Soak, Stress, And Chaos Testing
29. Local CI And Gauntlet Runner
30. Documentation And Release Materials
31. Code Quality And Maintainability

Do not work from this heading list alone. Open the spec and use the full acceptance details.

### 8. Seven Stages

The saved spec defines seven stages:

1. Stage 1 - 3.3: Install, First Run, Local Gate Foundation
2. Stage 2 - 3.3.x: Operator Workflow, Three Channels, Media, Recording
3. Stage 3 - 3.4: Control Room And Device Adapter Depth
4. Stage 4 - 3.5: Virtual Media Studio As A Reusable Lab
5. Stage 5 - 3.6: Migration, Archive, Civic Records, Producer Ops, Campus
6. Stage 6 - 3.7/3.8: Broadcast Expansion, Compliance, Resilience, Security
7. Stage 7 - 4.0: Integrated Release Candidate, Documentation Closure, Final Gate

The stages are order of attack, not permission to leave work incomplete. Stage 7 is done only when all 31 items are implemented, documented, locally verified, and covered by the final gate.

### 9. Recommended Immediate Next Steps

If the user says to proceed with implementation:

1. Re-verify repo identity and status.
2. Create a local work branch from `main`, for example:

   ```powershell
   git switch -c local/3.3-to-4.0-roadmap
   ```

3. Commit the new planning docs locally if the user wants the handoff/spec preserved in git:

   ```powershell
   git add docs/spec/3.3-to-4.0-sprint-list-and-implementation-plan.md docs/process/handoff-2026-07-02-3.3-to-4.0-coder.md
   git commit -m "docs: add 3.3 to 4.0 implementation roadmap"
   ```

4. Start Stage 1 from the saved spec.
5. Establish the local runner/clean-machine gate first.
6. Keep the installer substrate alive through every stage.
7. After each implementation slice, run relevant tests plus GauntletGate Lite and fix findings to zero.
8. At each stage end, run GauntletGate All on the full repo and all relevant components, fix findings to zero, update docs/evidence, and commit locally.

Do not push this branch, merge it, tag it, or publish artifacts unless the user explicitly says to.

### 10. Verification Expectations

For every stage:

- Start with source-state capture.
- Run relevant unit/integration/frontend tests.
- Run virtual lab scenarios related to that stage.
- Run clean-install or installed-product checks when a user-facing or runtime component changes.
- Use the clean Windows VM path for installer and installed-product claims.
- Produce artifacts under `artifacts/`.
- Update docs before calling the stage done.
- Run GauntletGate Lite after each slice.
- Run GauntletGate All at the stage boundary.
- Fix findings to zero before committing stage completion.

No skipped required check is acceptable as a green result unless an equivalent enabled substrate proves the same capability in that same report.

### 11. Current Handoff Files

This handoff file:

`C:\CivicCastTester\v3-beta-release-prep\docs\process\handoff-2026-07-02-3.3-to-4.0-coder.md`

Roadmap/spec file:

`C:\CivicCastTester\v3-beta-release-prep\docs\spec\3.3-to-4.0-sprint-list-and-implementation-plan.md`

Market expansion/deep dive report:

`C:\Users\scott\Documents\Codex\2026-07-01\i-need-a-deep-dive-into\outputs\civiccast-broadcast-market-expansion-deep-dive-2026-07-01.md`

### 12. Tone And Collaboration Notes

The user has been explicit that they are tired of repeated partial scoping. Do not answer with vague "we can do X later" language if X is buildable locally. Add it to the plan, implement it in the assigned stage, or explain the true external blocker.

The user wants plain English status. When reporting progress, say what is done, what is currently running, what failed, what is next, and where the evidence is.

When a task is active and the next step is clear, keep going. Do not stop merely to report that you know what to do.

### 13. Final Reminder

The next session should preserve the released `v3.2.0-beta1` baseline and begin the local 3.3-to-4.0 implementation from the saved spec. The goal is not another plan. The goal is to finish the software, docs, local lab, local tests, installer path, and release-candidate gate described by the 31-item spec.
