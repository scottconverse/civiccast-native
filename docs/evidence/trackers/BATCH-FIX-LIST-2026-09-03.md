# CivicCast batch-fix list (2026-09-03)

Collected from: Gate A evidence, the installer-path audit (Opus, running), the full UI walkthrough (running), and
owner observations. Everything here gets fixed in ONE PR batch, then ONE Gate A. Do not fix one at a time.

## Owner-observed
1. **Windows Sandbox: "Do you want to allow public and private networks to access this app? TSDuck (tsp.exe)"
   appears on every install test.** The prompt is inside the sandbox (default wallpaper in the screenshot), raised
   when `In-Sandbox-Report.ps1` runs `tsp` for the egress verify. Fix: the in-sandbox harness runs elevated, so add
   `netsh advfirewall firewall add rule name="TSDuck tsp" dir=in action=allow program="<TSDUCK_PATH>\bin\tsp.exe" enable=yes`
   (and the matching `dir=out`) BEFORE the first `tsp` invocation; also cover the sandbox-side `ffmpeg`/`ffprobe`
   if they listen. Proof: a Gate A run with no prompt (Scott confirms) and the egress-verify report unchanged.

## From Gate A 33681670855 (beta.2 -> beta.3) — fixed in #143, listed for the proof run
2. Pre-upgrade restore drill compared the old dump to the NEW schema head (rolled back before migrating).
3. Installer continued after a D3 rollback under the flat layout and exited 0.
4. Harness trusted HTTP 200 while /health said degraded/behind.
5. Drill read the old-schema DB through the new ORM (found by the new N-1 test).

## From the installer-path audit (84 findings: 13 BLOCKER / 41 MAJOR / 30 MINOR; report floatsom/CIVICCAST-INSTALLER-PATH-AUDIT-2026-09-03.md)
-> PR #145 @ 13eccc2 (65 files): 13/13 BLOCKERs, 30 MAJORs, 5 MINORs fixed; 8 MAJORs + 25 MINORs skipped with reasons in the PR body. Includes batch items 1 (TSDuck in-sandbox firewall rule) and 11-16 (test blind spots). Opus review + CI running; NSIS behaviour is proven only by the next Gate A run.

## From the UI walkthrough (2026-09-03, 24 screens, real backend; report: floatsom\CIVICCAST-UI-WALKTHROUGH-2026-09-03.md) - FIXED in PR #144, MERGED 1b871c3 (item 6 was already fixed on main; item 7 retested with Playwright: not a product bug)
6. MAJOR - first-run sample-video auto-packaging throws `'_EphemeralAssetStore' object has no attribute 'mark_packaged'` on every fresh dev boot (manual "Package for playback" works). Checked: the Postgres store (schedule/store.py:582) and app.py:1510 both implement mark_packaged; only the dev-only _EphemeralAssetStore lacks it -> dev-mode defect, keep as MINOR-for-production / MAJOR-for-dev-boot.
7. MAJOR - Publish dashboard "Approve and Publish selected" did not visibly complete (browser-automation click targeting unreliable; needs a Playwright retest before calling it a product bug). Blocks verifying Watch page + captions toggle.
8. MAJOR - public portal "Browse recordings" 503s under ephemeral/dev storage (`/api/public/search` 503 while `/api/public/assets` has the data). Checked: civiccast/metadata/router.py raises 503 _DB_NOT_READY by design whenever the metadata database is not there ('never a silent 200 against storage that is not there'); on a real station with Postgres it works. Dev-mode only -> MINOR. Optional UX: the public Recordings screen could fall back to /api/public/assets when search is 503.
9. MINOR - First Setup fires 8 unauthenticated staff-API calls before sign-in (8 console 401s).
10. MINOR - portal-public vite.config.ts has no /api dev proxy (undocumented), unlike portal-operator.

## From the tests/native audit sweep (2026-09-03) - test blind spots to close in the same batch
11. MAJOR - `tests/native/test_build_native_station_bundle.py:742-814` compares two bundles built by the SAME code at the same product_version; add a candidate N vs N+1 build and assert model packs identical + core differs (the #127 shape).
12. MAJOR - `tests/native/test_upgrade_backup_psycopg2_free.py:206-250` never asserts `expected_revision` reaches the drill; add it, plus the `read_db_revision -> None` fail-closed case (the #143 shape; today only a Docker-gated test guards it).
13. MAJOR - `test_upgrade_pg_lifecycle.py:157-186` proves a try/finally written inside the test, not the production finally; drive `upgrade_main.main` instead.
14. MAJOR - `test_upgrade_resume.py:172-184` (`>= 1`) and `test_upgrade_orchestrator.py:274` (`or`) are vacuous; `test_upgrade_routing.py:336-371` leaves `__main__.py:448-457` (journal.error -> reason line) untested.
15. MAJOR - `test_upgrade_cli.py:81-97` proves the flat-layout adapter was called, not that its result is used.
16. Coverage gaps: downgrade routing (new < old), DB AHEAD of code, second concurrent engine instance, lay_tree/junction-flip failure, real OSError(ENOSPC) on backup/lay_tree/journal write.


## Flaky-test candidates seen in CI (2026-09-03)
17. `tests/stream/test_media_router_storage_absent.py::test_vod_playback_degrades_instead_of_raising_500` failed once in randomized-suite (seed 3501300849, PR #151 run) and could not be reproduced with that seed on the PR branch or main (narrowed collection). Watch for a second occurrence; if it repeats, pin the order dependence (shared storage-absent fixture) rather than retrying.
18. `tests/live/test_finalization_worker_app_wiring.py::test_finalized_recording_is_playable_hls_over_local_serve` flaked under full-suite load (seen by two reviewers on 2026-09-02; passes standalone). Likely port/ffprobe contention.

## Docs wording (2026-09-03 16:05Z)
19. The beta.3 tester/README docs say a beta.1 station does 'one fresh install from the beta.3 kit'. Ambiguous: it means run setup.exe from the FULL kit (station folder beside it) OVER the existing station - data kept, no wipe. Reword everywhere (INSTALL-WINDOWS, lpm-beta-test-handoff, README, release notes) and state the one unsupported path plainly: setup.exe alone from beta.1.

## From the GStreamer fix (PR #153, 2026-09-03)
20. MAJOR - #151's control-plane logging is still not visible in production: uvicorn configures only its own loggers, so `civiccast.*` INFO records are dropped by logging.lastResort; and station-diag does not collect `<work_dir>/<channel>/logs/gst-worker.*.log` (where the worker traceback sat all along). Fix both.
21. MAJOR - `compliance.locate_tsduck` never looks in `<install>\packs
ative-server-binaries\payload	sduckin`, so the product cannot find the tsp it ships (the harness sets CIVICCAST_TSDUCK_PATH only in its own process); costs the relay splice feature.

## Mutation job (2026-09-04 01:05Z)
22. deterministic-detectors.yml mutation-report: `pytest_add_cli_args_test_selection` deselects do not apply to mutmut's stats run, so source-tree-binding policy tests (now: test_native_activation_selftest_layout_policy::test_self_test_server_binary_paths_are_all_builder_declared when scripts/build_native_server_pack.py is in the diff) fail the job. Either exclude those source files from only_mutate or make the policy tests resolve the pristine repo root (not the mutants/ copy). #156 merged with this red.

## Harness evidence (2026-09-04 07:15Z)
23. Gate A diag capture does not collect C:\ProgramData\CivicCast\provision\ (PROVISION-RECOVERY.md, journal) nor the nsExec stderr of d4-provision; the rc-75 diagnosis had to be reconstructed from log timelines. Add both to Invoke-StationDiagCapture.
24. Rust wrapper GENERIC_EXIT_CODE 75 collapses every provision CLI exit (0/10/20/30/40/50) to one number; surface the real code in install-progress.log.
25. Architecture (reviewer of #159): D3 should STOP the supervisor service before D4 runs (single ownership of pgdata); #159 is the local 'do not touch what you do not own' invariant. Residual: ADOPT_EXISTING when reused_usable is False still pg_ctl-sandwiches a live cluster.
26. PRODUCT: the GStreamer playout worker exits at every source-plan end (max_segments=8, source_plan.py) and is restarted by automation auto_start -> a channel blip every ~10-15 min under continuous premieres. Needs seamless rollover (lookahead_source_plan_provider is None in automation.py; reload_content exists on the gst strategy) — measured in the 120-min soak 2026-09-04.
27. HARNESS: three independent post-upgrade-proof defects hid behind each other (exit-code null #158, wrong registry key #160, unquoted psql SQL): the proof had never executed end to end since it was added on 09-02. Add a harness self-test lane that runs the proof against a known DB in CI.
27b. Fourth defect in the same proof: UNION over both namespaces (public.alembic_version absent). Item 27's harness self-test lane is now mandatory before beta.5.

## beta.5 follow-ups (2026-09-04 18:00Z, from the #162 final review)
28. graph.source_leg_is_clock_timed: docstring says unknown -> True but code returns False (unlisted live factories ksvideosrc/mfvideosrc/dshowvideosrc/wasapi2src/interpipesrc/v4l2src would be held+rebased); fix docstring + add factories + a graph test for an unknown factory.
29. T6 soak lane: add a per-boundary max FORWARD audio PTS gap metric (expected one AAC frame ~21-40 ms) and count worker exits/element count across >=50 rollovers; measure a held 1080p leg's RSS over a 120 s lead.
30. Engine: preroll-then-arm (arm the deferred switch only once the new leg reports ready) to remove the bounded freeze when a cold conform outlasts the 120 s lead.
31. Engine: immediate-switch path (switch_at_end_of_current=False) still does not rebase -> possible audio PTS leap on slate gap-replans/takeovers.
32. prune_local_candidate_roots.ps1 keep-list must include any kit currently junctioned by a live sandbox run (or the soak must copy the kit out of kit-staging first); a build during a soak deleted the kit under the sandbox (2026-09-05 00:36Z).

## beta.5 soak findings (2026-09-05 05:20Z, kit e502074)
33. Automation rollover (#162) never dispatched a reload in the T6 soak (worker logs: zero 'CTRL reload'); suspect the horizon/trigger path for API-started channels. Blocks beta.5.
34. With #162, the plain plan-end path STALLS (worker alive, no output, 10 s stall watchdog kill) instead of the beta.4 EOS -> clean exit; regression in engine.py's selector/EOS handling even when no deferred switch is armed. Blocks beta.5.

## CORRECTION (2026-09-05 05:40Z): items 26/33/34 re-characterized
Both soaks' restarts were 10-s OUTPUT STALLS (worker stderr 'CTRL stall: no output for 10s'), not plan-end exits; plans are 28-38 min, restarts 1-25 min apart. Real causes: (a) stalls of 3 software-encoded channels in the GPU-less sandbox while SourcePreparer conforms synchronously on the automation thread; (b) UnicodeEncodeError in _write_state on U+FFFD in last_error -> automation pass aborted for that channel (26/23 times). #162 never exercised.
35. PR #167: ASCII-safe child-log tail (fix for b). Follow-up: pin DB client_encoding=UTF8 (any accented asset title can crash a state write).
36. daemon._try_content_reload / SourcePreparer: conform OFF the automation thread (prepare-then-arm); today one cold conform blocks all channels' supervision for its duration.
37. T6 lane: record stall count + inter-stall interval + CPU/RSS at each stall as first-class metrics; pid-change counting cannot distinguish plan ends from stalls. Also: does the stall reproduce on real hardware (R7/iGPU)? Needs a station-hardware soak, not only the sandbox.
38. Beta.4 public docs mis-stated the known issue (plan-boundary restarts) -> correction PR in flight.
39. dr/backup.py fallback locale claims UTF8 'because provisioning creates it' — provisioning does not (no initdb -E); measure via pg_database; restore-drill encoding drift test. In the encoding PR.
40. Self-hosted runner: a build queued behind Gate A fails at actions/checkout cleanup with EBUSY on sandbox-lab\hoststore (twice on 2026-09-05). Add a pre-checkout wait-until-unlocked step or give Gate A its own workspace/runner label.
41. PRODUCT: first-admin 'already complete' is persisted in station-state.json under the SERVICE account's %LOCALAPPDATA%\CivicCast (civiccast/installer/station_state.py:488-496), which survives uninstall + removal of ProgramData\CivicCast + the install dir -> a reinstalled station answers 409 to first-admin with no admin credentials anywhere. Uninstall (or a 'remove station data' choice) must clear it; documented recovery path needed. Found on the tester 2026-09-05.
42. source_plan._segment_duration (source_plan.py:335) uses asset.duration_seconds and ignores item.duration_seconds -> a 30-s slot of long media plays for hours (schedule slots not honoured).
43. Rollover cadence: with short items max_segments=8 gives 4-min plans and a 2-min rollover cycle per channel, each with synchronous conform on the automation thread (CPU burn, stalls, restarts on the tester 2026-09-05). Fix: duration-based plan window (>=30 min), rollover min-interval, prepare off-thread.
44. health.py:42 reads the whole gst-worker.stderr.log twice per 2-s tick (O(filesize)); tail it.
45. PRODUCT (root cause of ALL soak restarts, measured on the tester 2026-09-05): the live caption tap (civiccast/captions/tap_worker.py) transcribes every ON_AIR channel in-process on CPU, overloads ('Caption tap overload ... exceeds the maximum 2' every 30 s on 3 channels), never backs off, burns ~2.5 cores in the control plane and starves the GStreamer workers into 10-s stalls/relaunches. Fix: overload backoff/pause, ASR concurrency + CPU bounds, playout worker priority ABOVE_NORMAL. Items 26/33/34/36/37/43 were symptoms/mis-attributions.


## 46. mutation-report lane baseline is broken (2026-09-05)
- Job `mutation-report` (deterministic-detectors.yml, 'always informational', NOT a required check) fails its baseline run on every code PR today: tests/test_hermetic_state_guard.py::test_guard_fails_a_test_that_writes_real_state gets 2 errors instead of 1 because mutmut's plugin activates inside the nested pytest run (`FileNotFoundError: Could not figure out where the code to mutate is`). #135 (41045e7) meant to disable mutmut in that nested run; under mutmut 3.6 it no longer does. Evidence: run 33969237745 (#172 eda00c3). No code PR has a green mutation-report in the last 12 runs (docs PRs pass because nothing is mutated).
- Fix: make the nested pytest in tests/test_hermetic_state_guard.py run with `-p no:mutmut` and a cwd-independent mutmut config, or exclude that test from the mutation baseline like the other five deselects. Then prove the lane produces mutation data on a code PR.
- Not a release blocker; #172 was merged on the 20 real checks.


## 47. Upgrade install leaves manually-started channels dark; Gate A upgrade lane never checks on-air (2026-09-05)
- Measured on the tester: silent install of kit 91caebc over the running e502074 station succeeded (installer exit 0, /health healthy, PGDATA preserved) and the three channels (configured with auto_start=true by the soak) came back on GStreamer by themselves, in FALLBACK_SLATE because soak #1's schedule had run out. CORRECTED 19:05Z: the gap is for channels started by hand WITHOUT auto_start (they stay off until Start), per the code path below; not measured on this station. Cause: civiccast/egress/automation.py:478-493 re-issues `start` only for channels with config `auto_start=true`; nothing restores the last persisted egress_states.state after a service restart. Channel state = Postgres rows (egress_states / egress_configs, migration 0020), untouched by the upgrade.
- Gate A cross-version lane asserts only install exit / D3 route / schema revision / D4 exit / DIRTY_PGDATA_PRESERVED (scripts/gate_a_verdict.py:829-902); no ON_AIR check after install-over. T4 starts a brand-new channel.
- Fix options: (a) on service start, re-issue start for channels whose persisted state was ON_AIR/FALLBACK_SLATE at shutdown (resume-last-state), or (b) make the operator console's Start set auto_start=true by default for 24/7 channels + a plain 'Resume after restart' toggle; plus (c) add an ON_AIR-after-upgrade assertion to the Gate A cross-version lane. A station that goes dark on every upgrade until someone clicks Start is not out-of-box WOW.
- Not a beta.5 blocker (same behavior in beta.4). Document in beta.5 known issues.


## 48. Install-over with the same version string never replaces the app payload (2026-09-05)
- Measured: kit 91caebc (#172) installed /S over the e502074 station (both 1.0.0-beta.5): installer exit 0, healthy, but 70 min later the control plane still ran the pre-#172 caption tap (old CRITICAL text, runtime-status 'overloaded'). Repo cause: civiccast/apps/installer/src-tauri/src/native_pack_staging.rs classify_dest_pack_state -> AlreadySatisfied (:189/:741) when the on-disk pack's manifest product_version equals --new-version; ensure_pack_extracted (:645-656) then idempotently returns; nothing compares the incoming pack's sha/content. D3 SAME_VERSION_NO_OP (routing.py:341) is only the migration gate.
- Customer path beta.4 -> beta.5 (different strings) DOES replace the payload (Corrupt -> ReplaceCorruptFromOffline -> rebuild). So not a beta.5 customer blocker, but every candidate-over-candidate test on the tester was silently a no-op for the code.
- Fix: key pack staging on pack identity (manifest sha / station-index sha) not the version string; log 'payload replaced' vs 'payload unchanged' explicitly in install-progress.log; AND give every candidate build a distinct version (e.g. 1.0.0-beta.5+<sha> build metadata) so two kits never share a string. Add a Gate A assertion that after install-over the installed civiccast package hash equals the kit pack's.


## 49. Gate A has no same-version install-over lane (2026-09-05)
- The cross-version lane's baseline (sandbox-lab/upgrade-baseline.json) is always a different version string, so the item-48 regression (same string, different content) cannot be produced by any Gate A lane; gate_a_verdict.py:844-850 even rejects D3_ROUTE=SAME_VERSION_NO_OP. Add a lane: previous kit = a differently built kit at the SAME product_version (e.g. the prior candidate from kit-safe), install over, assert the installed app-payload digest equals the new kit's and that a marker file present only in the new build exists. Until then, same-version coverage is the Rust e2e test in #173 only.


## 50. Test isolation: a claims-evidence test writes the operator's real installer-state.json (2026-09-05)
- tests/policy/test_claims_evidence.py::test_ws3r3_005_drifting_one_bound_code_module_independently_invalidates_the_claim[...civiccast/egress/gst/worker.py] tripped the hermetic teardown guard: it wrote the real C:\Users\scott\.civiccast\installer-state.json instead of tmp_path (seen on HALO while running tests/docs tests/policy for #165; pytest still exits 0). Fix: route that test's installer-state path through tmp_path/monkeypatch like its siblings; make the guard fail the run, not just error in teardown.


## 51. Plan window x per-segment decoder chains = decoder blow-up (2026-09-05, REGRESSION from #170)
- #170 set PLAN_MIN_SECONDS=1800 / PLAN_MAX_SEGMENTS=120; with 30-s schedule items a plan is 60 segments; gst/bridge.py builds one filesrc->decodebin chain PER segment in one pipeline (engine._build_playlist adds all, PLAYING at once); avdec_h264 ~20 threads each -> measured 1238 threads / 3.5 GB per worker, no output within the 10-s stall watchdog -> relaunch every ~30 s on the tester (clean 91caebc). Pre-#170 = 8 segments.
- Hotfix PR (fix/plan-window-decoder-blowup): cap segments at 8, PLAN_MIN_SECONDS 0, rollover floor derived from plan length, hard cap on sub-chains in the bridge. Durable fix: bounded lookahead of sub-chains built on demand so plan length is not a multiplier on decoders/threads/RSS. Also: ABOVE_NORMAL worker priority (#172) + synchronous SourcePreparer on the automation thread = priority inversion aggravator; consider preparing off-thread or dropping ABOVE_NORMAL.


## 52. Harness: ffprobe not found on the tester -> 30-s default item durations (2026-09-05)
- AUTORUN-9e/9u probe candidates ($kit\packs\...fprobe.exe, C:\CivicCastHostStore\install\packs
ative-server-binaries\payloadfmpeginfprobe.exe, installfmpegin, Program Files\CivicCast (Native)\...) all missed on DESKTOP-VBMA6O5, so every schedule item defaulted to 30 s (a 30-s trim of each clip). That schedule shape is what exposed item 51, so keep it as a stress case, but the harness should (a) find ffprobe where the installed station really keeps it (locate via the station's own runtime: `install
untime\...` or the native-ffmpeg-runtime pack payload), or (b) read duration_seconds from the asset record after packaging (9m did; it carries the real duration). Also: the product API should return the packaged duration on GET /api/staff/assets/<id> so operators never schedule a 30-s slot for a 40-min clip by accident.


## 53. Bound avdec_h264 decoder threads per sub-chain (2026-09-05, follow-up to 51)
- No max-threads is set anywhere under civiccast/; avdec_h264 defaults to auto (= ncpus) so each playlist sub-chain costs ~20 threads (measured 1238/60). Even the fixed 8-sub-chain plan is ~165 threads per worker, ~500 across three channels on 8 cores. Set max-threads=2 (or ncpus/subchains) in civiccast/egress/gst/decode_policy.py; that file is D2 blob-drift-bound in docs/claims/claims.yaml, so re-provision the claim. Durable fix: build sub-chains lazily (bounded lookahead) so plan length never multiplies decoders.


## 54. Planner/rollover accounting after fallback: negative plan-end, stuck TRANSITIONING (2026-09-05, soak #4 evidence)
- control_plane-app.log 22:32-22:56Z (DIAG-9zz): after the stall relaunches, channels fell to FALLBACK_SLATE with 'No valid source plan is available' (schedule gap between the last 30-s item and the first long item), government tripped 'Live source failed to stay on air after 5 consecutive crash-relaunches; aired fallback slate'; then automation logged 'seamless plan rollover for government: the live plan ends in -1208s and the schedule continues 4367s further' (negative plan end = horizon still anchored to a dead plan), and after the 22:53/22:54 rollovers ('schedule continues 0s further', 1 segment) all three channels stayed in TRANSITIONING (state rewritten every 2 s, same pids) for 20+ min. Investigate: (a) horizon reset when a channel leaves ON_AIR (automation.py rollover floor is cleared on non-ON_AIR per #170 review, but plan_end is not), (b) content-reload that never completes leaves TRANSITIONING forever with no watchdog, (c) end-of-schedule rollover with 0 s continuation. Reproduce in tests/egress with a schedule gap + a short tail.


## 55. Pending content-reload latch has no timeout, no watchdog, no operator recovery (2026-09-05; RC1 of item 54)
- daemon._request_reload (daemon.py:1670-1709): when _try_content_reload fails (gst/strategy.py send_and_wait 5-s ack timeout, _WORKER_PIPE_ACK_TIMEOUT_S=5.0 at strategy.py:91), it sets _pending_reloads[channel] and writes TRANSITIONING, but for an ON_AIR channel never terminates the worker; _poll_process:1178-1179 rewrites TRANSITIONING every tick with the same pid; the latch clears only at :1222/:1258/:1721/:1744. Automation is disabled meanwhile (_check_plan_rollover returns on non-ON_AIR at automation.py:716-727; the B2 retry at :779-798 sits inside the ON_AIR-only branch = unreachable). runtime_status seconds_in_state resets on every identical write (alerting/runtime_status.py:54-58) so the UI shows ~2 s. Present in beta.4 (daemon.py unchanged by #174).
- Fix: record _pending_reload_at; in _poll_process alive branch, after N x ack timeout pop the latch, restore the previous state row, WARNING + command_failure_hook; do not bump updated_at on a no-change write / track state_entered_at; make the B2 retry reachable; add a TRANSITIONING-duration alert. Tests: fake strategy with reload_content False + never-exiting process -> row returns to ON_AIR, latch empty, rollover dispatches again; seconds_in_state monotonic.

## 56. Slate/fill plans truncated to 12 sub-chains = slate encoder restart every ~6 min (2026-09-05; consequence of #174)
- SlateSourceGenerator builds 120 x 30-s repeats (source_plan.py:91-102, target_fill_seconds=3600); bridge now truncates fill plans to MAX_PLAYLIST_SUBCHAINS=12 with a WARNING -> EOS after ~360 s -> worker restart during every slate period (relaunches_total counts it). Fix: generate fill plans as <=12 segments of longer duration (one 300-s slate segment repeated 12x, or a looping slate source) so a slate period never EOSes inside an hour.

## 57. Rollover horizon guards (2026-09-05; RC4 of item 54)
- automation.py:800-860 dispatches a rollover even when plan_end_at - now is negative (logged '-1208s'); :841 fresh_end <= plan_end_at has no epsilon (0-s 'continues 0s further' rollovers cost a full synchronous prepare). Fix: require advance >= rollover lead; WARNING + no dispatch when remaining <= 0; table-driven tests.


### 55 correction (2026-09-06 00:40Z)
- The long TRANSITIONING is the DESIGNED graceful drain (daemon.py:1770-1776): a program reload whose seamless path failed waits for the plan's natural EOS, then restarts on the new plan. Real defects: the state is mislabelled/undated (operator cannot tell a 20-min drain from a hang), runtime_status resets time-in-state on every write, automation pops the horizon during the drain (no rollover bookkeeping), the B2 retry is unreachable, a lost ack is not surfaced, and slate reload retries can flap every 30 s. #175 is being redone on that basis; no flat timeout.


## 58. Gate A cross-version lane: phase-1 (baseline install) has no forensics and no retry (2026-09-06)
- Run 33998901590 on 609273d: DIRTY-PREP-RESULT.txt PHASE1_INSTALL_EXIT=-1073741819 (0xC0000005) while installing the baseline beta.4 kit inside the sandbox; the artifact carries only phase-2's install-progress.log, no phase-1 log, no WER crash dump, no installer stdout/stderr. The same lane passed 9 h earlier with the same baseline bytes (sha 9fae1211 verified). Fix: (a) capture phase-1 install-progress.log + the NSIS log + WER dump path into the artifact under phase1/; (b) on a non-zero phase-1 exit retry once after a fresh sandbox reset and record both attempts; (c) teardown drain timed out on a busy handle in kit-staging\c27c6e7 (TEARDOWN-DRAIN-TIMEOUT.txt) -- find which process holds it.


## 59. Fresh station: first start takes >3 min to ON_AIR (2026-09-06)
- Twice (9u on 91caebc, 9zj on 609273d) a freshly installed station's three channels did not report ON_AIR within 3 min of the first start command (raw /state empty/STARTING), then were ON_AIR by the next poll. Harness: wait 6 min (9w/9zl do). Product: surface first-run warmup (model load, pack unpack, first decode) as STARTING progress in the console; measure what the 3+ minutes are.


## 60. Seamless in-place plan extension (#162) stalls the live GStreamer pipeline (2026-09-06, soak #5 on 609273d)
- With #174's scaled floor, rollovers fire every ~3-4 min on 8x30-s plans and the daemon applies them in place (Content-reload 8 segments, 0.3 s). Right after the first reloads (02:55-02:56Z) every worker began exiting with `CTRL stall: no output for 10s` about every 30 s (government hit the 5-crash guard). Before the first reload the 30 min were clean; workers stayed small (550 MB / 178 threads), so this is not item 51. #162's reload_content path was never exercised on hardware before (soak #1 never rolled over). Diagnosis in progress (Opus). Fix options: make the extension safe (blocked pads until preroll, drop played sub-chains, live sub-chains <= cap), or fall back to terminate+restart at plan end for GStreamer until proven, or stop rollovers firing. Blocker for beta.5 on 609273d.


### 60 root cause (2026-09-06 03:45Z, Opus, code-proven)
- bridge.py:639 labels every program PlaylistLeg 'program'; engine._build_playlist:361-363 names concats vconcat_program/aconcat_program; reload_program builds the new leg while the old is live -> pipeline.add() refuses the duplicate names (return ignored in _make:~285) -> new sub-chains dangle -> hold probes never fire -> _on_reload_timeout (10 s) aborts EVERY reload; worker.py:175-183 acks 'applied' before readiness -> daemon thinks it landed -> automation re-issues forever. Overlay path already uses _overlay_layer_seq. Tests only reload SourceLeg, never PlaylistLeg. Plus H2: a reload doubles the live decoder tree (cap is per plan, not per pipeline) -> encoder starved past the 10-s watchdog.
- Fix PR fix/gst-reload-concat-collision: fail-loud add, unique concat names, seamless reload gated OFF by default (env CIVICCAST_EGRESS_SEAMLESS_RELOAD=1), honest worker ack.

## 61. Worker reload ack is dishonest (2026-09-06)
- worker.py:175-183 acks 'applied' as soon as reload_program returns (before commit/abort). Carry the outcome; daemon must restart when a rollover never yields a new proof_event_id instead of re-issuing. (In the item-60 PR.)

## 62. Decoder-chain cap must be per PIPELINE, not per plan (2026-09-06)
- A reload transiently holds old+new legs (8+8, up to 12+12). Enforce a live total cap in the engine (build the new leg blocked, dispose played sub-chains first) and set avdec_h264 max-threads (item 53). Beta.6, with a hardware soak showing `CTRL reload committed` in worker stdout.

## 63. Harness: capture gst-worker.stdout.log (2026-09-06)
- All reload progress prints go to worker stdout (`CTRL reload ...` lines); the daemon only folds stderr into last_error and my diags only tailed stderr. Every diag/soak evidence bundle must include the full stdout+stderr per channel, and Gate A should archive them.


### 60 root cause CORRECTED (2026-09-06 03:45Z)
- Hardware: worker stdout has no `CTRL reload` lines, stderr no name collisions -> the reload never reached the worker. Confirmed by code: preparer.py:342/378 writes prepared segments to `prepared/segment-NNNN.ts` keyed by channel only, in place, non-atomic (for GStreamer: :246-268, :465-476); a content-reload prepare overwrites the files the LIVE worker's filesrc is reading -> stall. And daemon.py:1617-1618 returns False on a failed send with NO log line (item 65). The name collision (H1) is real in code but was never reached here.

## 64. Content-reload prepare clobbers the live plan's segment files (2026-09-06)
- Fix in the item-60 PR: per-plan prepared directories, tmp+atomic replace, GC after the worker exits; tests that preparing plan B never touches plan A.

## 65. Silent reload failure (2026-09-06)
- daemon._try_content_reload returns False with no log when send_and_wait fails; add WARNING with reason. In the item-60 PR.


## Added 2026-09-06 06:55Z from sandbox soak run 5 (kit 609273d, Opus diagnosis; evidence cc-sbsoak\sandbox-lab\soak-output\soak-609273d-20260906-061849Z)

66. **CRITICAL - cold full-asset conform runs synchronously on the single automation thread and blocks every channel.** `automation.py:536` process_once inline in run_once's per-channel loop (one thread, all channels); `daemon.py:686-690` _start calls the preparer inline; `preparer.py:449` -> `_conform_full_asset_into_cache` (docstring :183 "Conform the WHOLE asset (no trim)", segment=None :192, background=True :198 single-threaded encode). A 30-s slot of the 2366-s/819 MB 1080p clip pays a full 2366-s re-encode. Evidence: education segment-0001 06:33:34, segment-0002 06:41:33 (7 min 59 s gap); government's whole prepare 0.56 s afterwards (shared cache). No log line on the start path (reload path has one at daemon.py:1573). = the measurement item 59 asked for. Fix: log prepare start/finish + per-asset conform time (1 h); prepare off-thread per channel (1-2 d); conform only the needed window or warm the cache at ingest (2-3 d). beta.6.
67. **HIGH - no state row until AFTER prepare; GET /state returns 200 null for minutes.** `daemon.py:702` first STARTING write sits after the preparer call (:686-690); router.py:1007-1017 returns None -> null. Evidence: 33 consecutive null polls 06:33-06:41. Fix: write PREPARING/STARTING before plan resolution + prepare; typed "no state yet" instead of null (2 h). beta.6.
68. **MEDIUM/HIGH truth - ON_AIR persisted before the worker exists.** `daemon.py:730-736` writes ON_AIR pid=None BEFORE `_encoder_strategy.start` (:759). Evidence: STARTING 00:41:46.826 -> ON_AIR(pid=-) .833 -> "starting GStreamer worker" .861 -> ON_AIR(pid=10672) .909. NOT item 61 (that is the worker's reload ack). Fix: stay STARTING until encoder_result.process exists; :829 the only ON_AIR write (1 h + tests). beta.6.
69. **MEDIUM-HIGH - 53 redundant state writes per channel start, nulling current_proof_event_id.** dispatcher.py:96-99 picks "start" whenever the row is not running (a null row always reads dark) and commit_service.py:448 dispatches once per committed item -> 50 commit-start commands per channel; store.py:355-377 pop_pending_commands has no LIMIT; daemon.py:593-615 already-live early return rewrites state without current_proof_event_id (:599-604, the only ON_AIR write with pid but no proof id); daemon.py:863-919 _write_state has no compare-to-previous guard; store.py:379-391 full-row replace. Evidence: exactly 53 ON_AIR lines per channel in 1.5 s; final row current_proof_event_id=null on a live channel; automation.py:757-759 keys the rollover horizon off that field. Correction: a bounded burst (1.5 s), not a sustained storm. Overlaps item 55 (no-change write guard). Fix: no-op _write_state on no change; preserve proof id in the early return; dedupe queued starts; bound pop_pending_commands; no start-per-committed-item (2 h + 1 d). beta.6.
- Decision (Fable, 06:55Z): beta.5 stays = #176 only. 66-69 are one-time start-path costs the 15-min lane tolerates (10-min ON_AIR bound); if run 7 on the #176 candidate fails on any of them, they move into beta.5.

70. **(FIXED in #179 before release) gst-python Bin.add contract misread.** #176 wrote `if not self.pipeline.add(element)`; gst-python's override returns None on success and raises Gst.AddError on failure, so the first real add always raised 'duplicate element name'. Caught only by the build's real-GStreamer smoke; fake pipelines modeled a bool. Lesson: every fake of a gi override must model the override's contract, not the C API's; the smoke step is load-bearing.
71. **Pad.link raises Gst.LinkError under gst-python; 8 `!= Gst.PadLinkReturn.OK` branches are dead.** engine.py:1007 audio branch of `_link_leg_to_selectors` has no try/except, so a failed audio link during a seamless reload escapes before the two request pads are released (pads leak on the channel-lifetime input-selector, once per failed reload) — the F2 cleanup is defeated. Also :392, :415 (pad-added callbacks: PyGObject swallows => the audit-M3 WARN can never fire), :465, :476, :502, :630, :684. Element.link and Bin.remove DO return bool (not overridden) — leave those. Pre-existing since 714b1da. Fix shape: a `_pad_link()` helper like `_bin_add` (catch Gst.LinkError only) at every site + a fake pad that raises. Also: engine.py:316/:355 + claims.yaml say 'GStreamer 1.28.5' but the closure runs 1.28.6 (wheel vs library). beta.6 unless a flag-ON sandbox run shows reload link failures.
72. **(harness, latent) Run-GateA.ps1:68 and gate-b/Run-GateB.ps1:63 `[string]$Root = $PSScriptRoot` param default is EMPTY under Windows PowerShell 5.1 when the param block has a Mandatory parameter** (reproduced 2026-09-06); both workflows use pwsh so CI is fine, but an operator running either under powershell.exe crashes at Join-Path. Fix: default '' and resolve $PSScriptRoot in the body (as sandbox-lab/Run-SandboxSoak.ps1 now does).
- 2026-09-06 11:35Z: ITEM 66 MOVED TO BETA.5 (run 11 on candidate 3b: >12 min with no state row; the sandbox lane cannot even reach the soak). Fix shape per diagnosis in progress.
73. **Channels start serially on one automation thread** (automation.py:536 process_once inline per channel): channel #3's first ON_AIR waits for #1 and #2's prepares. After 66 A/B this is minutes not tens of minutes, but a per-channel prepare thread (or slate-until-ready) is the right end state. beta.6.
74. **Per-plan prepared dirs retain up to 3 full copies of every non-live segment per channel under a 5 GB budget, and `_dir_size_bytes` rglobs all of it on every prepare** (preparer.py:563). Disk + IO cost over a 24-h soak; not a latency cause. beta.6.
75. **(FIXED in #180 round 2) loudness probe per segment, decodes video, no memo.** preparer.py:684-688 `check_streaming_loudness` ran a full-file ebur128 pass (46.7 s on the 39-min clip, no -vn) for EVERY segment of the same asset: 8 x 30-s segments = 6.3 min on the synchronous start path. Memoized per asset in the cache meta + -vn.
76. **(FIXED in #180 round 2) warm scheduler unbounded** (preparer.py:94-96 thread per job; N cold assets = N concurrent -threads 1 encodes) -> single-worker queue. And the untrimmed foreground conform re-ran as a warm (double encode) -> promote into the cache instead.
77. **(harness, tester) AUTORUN-3.ps1 reads TSDuck counters at `$ts.*` but analyze --json nests them under `ts.packets.*`** => `$clean` always true, zero-packet gate never fires => every tester 'tsp pass' since soak #1 proved only 'tsp exited 0 with a parseable report'. Evidence: tester-soak5 egress JSON shows packets_total as an object and null counters. Fix in the next directive's AUTORUN-3; also re-read past soak verdicts with this in mind (relaunch counts stand; tsp claims do not).
78. **BETA.5 BLOCKER (run 12, candidate 3b, seamless ON): plan already ended at first ON_AIR (item 66 delay) => seamless rollover armed at once with 'live plan ends in -698s' => worker CTRL stall (no output 10 s) => exit => relaunch => automation re-arms => crash loop every ~65 s on all channels.** 26 relaunches / 15 min. Diagnosis in progress: stale plan at _start (re-anchor to now?) and/or the deferred switch at EOS blocking output. Evidence: cc-sbsoak-run\sandbox-lab\soak-output\soak-bcb3ebe-20260906-113951Z.
- 78 refined: NOT 'seamless arm stalls the worker'. 78a automation frozen horizon (automation.py:465/480/520/538/1047-1051 + guard at :910); 78b 45-s retry bypasses cadence floor (:920-935/:947-960); 78c should_defer_switch has no plan_end (reload_policy.py:116-126) -> 0/31 commits. Fix PR fix/rollover-horizon-item78.
79. **BETA.5 BLOCKER: caption tap overload starves playout in the sandbox VM (candidate 3b): 10 'Caption tap overload' events; worker stalls cluster inside them; the tap self-pauses 60/120/240/480 s but too late.** Same root cause as the tester 09-05 (beta.4). #172 insufficient. Fix shape: pause the tap on the FIRST stall warning; cap tap workers to 1 station-wide; lower tap priority. Isolation run with captions OFF pending.
80. **(harness) lane classifier ignores worker stdout** ('CTRL reload aborted/committed', 'CTRL stall') => reload_aborted_count 0 while the worker logged an abort; fixed in the lane follow-up.
- 79 refined: stalls persisted while the tap was paused 480 s => not the sole cause; candidate second cause = 78b/c re-arm storm (second 8-decoder leg on every fresh worker) and/or VM contention. Isolation runs (captions ON/OFF on candidate 3c) decide. Cheap tap change regardless: default_max_channel_workers() -> 1 (tap_worker.py:126-146).
81. **Suspect: offline caption job worker (app.py:1394-1400) transcribes uploaded/published assets in the SAME process as the control plane with cpu_threads=0 (all cores) and beam_size=5 while channels air.** If the lane's asset uploads/publish enqueue offline caption jobs, that alone can starve playout. Must be measured (jobs active per cycle) in the isolation runs. If confirmed: throttle/pause offline captions while any channel is ON_AIR (beta.5) or run it in a separate low-priority process (beta.6).
82. **Worker dies at start: `pipeline did not reach PLAYING within 5.0s (get_state=async)` (engine.py:1142 _await_playing) under load** (run 13, government first crash). A 5-s preroll bound on a CPU-starved box guarantees a relaunch storm. Fix: raise to 30 s (or until the stall watchdog would fire) and log the state-change progress; the daemon should treat a preroll timeout as retry-with-backoff, not crash-relaunch. beta.5 candidate (same family as 79/81).
- 81 RESOLVED as not-a-factor in the lane: the lane never calls approve/publish, so the offline caption worker polls idle (run-12 log: worker started, zero jobs). Live caption tap (egress audio fork, always on while ON_AIR) is the only in-process CPU competitor. 81 stays as a beta.6 item for real stations (approve triggers a whole-cores beam-5 transcription while channels air).
- 79 DEMOTED (run 14, candidate 3b, live captions OFF via the station profile, seamless ON): 6 unplanned relaunches in the first 4 min of the soak => the caption tap is not the driver. #182 = knob hardening only (beta.5 or beta.6, low risk). Remaining drivers: 78 (re-arm storm), 82 (5-s preroll under load), VM contention.
- 79 CLOSED as not-the-driver (run 14: 0 overloads, 21 relaunches). #182 stays as low-risk knob hardening. 78 is the primary beta.5 blocker; 82 secondary.

- **83 (beta.6, test hygiene)** `tests/stream/test_media_router_storage_absent.py::test_vod_playback_degrades_instead_of_raising_500` fails under pytest-randomly seed 1681376346 (VOD playlist route returns a bare 500 when a prior test leaves state behind). Also seen: `tests/test_cli.py` egress-runner lambdas failing with unexpected kwarg `playout_trim_supported` under seed 3559440525 on #180 e172f89. Replay each seed, find the polluting test, fix the fixture isolation. randomized-suite stays informational until the owner promotes it.
- **82b (beta.6, from #183 r4 MERGE follow-ups)** offset test must not rely on the pid backstop; resolve the spawn offset against the strategy's returned stderr_path (no 0 fallback after STOP->START); test the 4 MiB scan cap; warn on pid-mismatch rejection; fix CHANGELOG cost figure (158 ms at cap); bound daemon.py:1327 crash-path read_text; close FfmpegProcessHandle on natural exit.
- **66b (beta.6)** thread the plan's DB-known `asset.duration_seconds` (source_plan.py:523-543) into the segment/PreparedSegment spec so the preparer stops re-deriving it via an independently-fallible ffprobe; round 8 of #180 is the fail-closed stopgap.
- **78b (beta.6, from #181 r8 MERGE follow-ups)** fix 4 stale daemon.py comments (2171, 2156, 463, 1994) that contradict the shipped pop-only-on-match rule; key `_rollover_plan_end_at` per command (dict or bounded ring) so a retry cannot overwrite leg A's recording (A currently defers on a dead boundary for one tick until B supersedes); add a supervisor-subclass test that a mismatch-retained entry cannot survive an off-air/back-on-air cycle.
- **Lane follow-up D (harness, low)** from #185/#186 reviews: role label must not assert ffmpeg-fallback for non-ffmpeg names; anchor the DISPLAY_NAME drift regex; guard the injected Test-Path; reject non-positive poll interval; workflow fork if:/push main; stderr+stdout counter same-first-line truncate double-count + unit tests; harness_notes cap marker; README suite list -> point at Invoke-LaneUnitTests.
- **Docs drift (fold into #165)** ADR-0021 and two evidence memos cite `gstreamer-bundle==1.28.5` (no such distribution in either requirements file; the real pins are `gstreamer-*==1.28.5` MSVC wheels + `gstreamer-cli==1.28.5` sha pinned in BOTH requirements-native-runtime.txt and scripts/verify_native_runtime_closure.py:93-94); docs/design/spec-packaging-closure.md:12-15 says 'MSVC runtime installer' (it is wheels). Old Linux container was 1.28.4.

- **84 (beta.5, LANE-BLOCKING)** engine.py stall watchdog: one 10-s budget from PLAYING even before the first buffer; split into first_output_timeout_s (45 s, own exit code/receipt) vs 10-s stall after first output; automation.py 45-s 'did not land' WARNING every 2 s = noise. Evidence run 15 + 3 seamless-OFF runs.
- **85 (beta.5, seamless)** _commit_reload switches active-pad before releasing hold probes -> streaming thread wedged; _dispose_source_leg set_state(NULL) before unlink/flush; no commit watchdog; daemon leaves the wedged pid alive and pins TRANSITIONING. Evidence: 'boundary switch rebased' is the last line in runs 14/15; 0 commits in 7 soaks.
- **86 (beta.6)** _write_state bursts ~60 ON_AIR rows/s at spawn (education 12:50:35-37 in run 15), each a Postgres write; write/log on change only (extends item 67).
- **87 (beta.6)** tsduck_verdict fail-timed-out on every sampled channel/cycle in runs 14/15 even on live channels: the lane's tsp sample is not a usable output-presence instrument; fix the sampler (timeout/bind) before trusting it.
- **85b (beta.6, from #188 r2)** implement or drop the code-5 claim in installed_gstreamer_smoke.require_clean_worker_result; thread returncode through _service_backoff_relaunch; shared constant for the 'ack timeout' reason prefix + pinning test; document CIVICCAST_RELOAD_COMMIT_TIMEOUT_S in runbook + USER-MANUAL; fix the CHANGELOG rationale.
- **84b (beta.6, from #187 r2)** 4 stale doc sites claiming PLAYING is the on-air evidence; dead branch engine.py:1347; worker_reached_playing has no production caller; runbook 'within a second'.
- **88 (beta.5, LANE-BLOCKING candidate)** caption audio tap leg can block air: non-leaky queue + appsink drop=False + os.fsync on the streaming thread (engine.py ~972-1031, audio_tap.py). Fix: leaky queue, drop=True, writer thread. Evidence run 17.
- **84c (beta.5)** first-output marker fires at preroll (tautology); latch only buffers observed after arming; add a 5-s `CTRL output: N buffers` progress line.
- **89 (beta.6)** daemon reaps a dead child only on the 30-s automation tick (ON_AIR with a dead pid up to 30 s).
- **90 (harness)** conform-progress collector reports prepared: [] while playout-graph.json names the prepared dirs (enumeration bug).
- **91 (beta.5)** no shipped off-switch for the caption tap leg: station_runtime.py:1362 hard-injects CIVICCAST_CAPTION_TAP_DIR; add CIVICCAST_CAPTION_TAP=off + make live_captions_enabled=false drop the leg at the next worker start.
- **88b (beta.6)** audio_tap.py close() try/finally + re-entrant result; skip tap close on the force_exit_on_hang branch (or residual budget) and fix the 'comparable not compounding' + worker.py:554 prose; give consecutive_publish_failures a reader or drop it; make sequence exhaustion non-fatal to air; rename two stale tests.
- **92 (triage)** test_offline_caption_job.py::TestTruncatedSpanishTrackNeverPublishes::test_an_orphaned_spanish_row_neither_gates_nor_reaches_the_track: red 3/3 on main 5978ed5 in one clean worktree, green in another; env/date dependent; find the dependency.
- **92 ROOT CAUSE** civiccast/captions/review.py InMemoryCaptionReviewStore.list(): (created_at, review_item_id) sort ties on same-microsecond timestamps; use a monotonic insertion sequence as the tie-break. Small fix; do it in beta.6 hygiene.
- **91b (beta.6)** tap_worker main() should exit 0 with 'tap is off' instead of ValueError under CIVICCAST_CAPTION_TAP=off; fix station_runtime.py:1385-1390 + strategy.py:867-869 comments; CHANGELOG count; ops doc note on default-to-enabled when station-state is unreadable.
- **Lane follow-up E (harness)** from #189 r7: per-checkpoint wall-clock budget across gst-debug candidates; correct/omit the banner on a partial truncated capture (and keep the NEWEST bytes); ValidateRange on -WholeCopyDeadlineSeconds + mid-copy content test; In-Sandbox-Soak.ps1:942 precision; move Format-MBPrecise into GstDebugTail.ps1 with tests; delete .partial orphans on throw.
- **92b (beta.6)** review store: replace the _sequence counter with a stable sort by created_at (or itertools.count + write order), fix the persistence.py comment, add tie tests across approve/edit/reject and filters.

- [ ] 93 (beta.5.1): installer activation CLI stderr (the exit-67 reason) is discarded under /S; nsis-hooks-bootstrap.nsh:1397 promises it in install-progress.log. Append the CLI's stderr/last lines to install-progress.log on non-zero exit. Found 2026-09-10 via Gate A 34412708089.
- [ ] 94 (beta.5.1): Gate A activation self-test probes bounded at 30 s (main.rs:5536) trip under host I/O starvation; consider a longer bound or a retry for the python import probe, and record probe timings in activation-self-test.json.
- [ ] 95 (beta.5.1): PR #202 round 4 (399c4618) needs: loaded-run tally (20+ at 100% CPU), full egress/policy/captions suite, sandbox soak proving holds-released precedes old-leg-disposed on every rollover, then hostile delta review.
- [ ] 96 (beta.5.1): live caption embed root cause (cccombiner/CaptionGapGate video hold 25-30 s then burst; soak 2026-09-09) -- GST_DEBUG=cccombiner:6,aggregator:5 discriminator run via -WorkerEnv, then fix; re-enable captions by default only after a clean captions-ON soak.
- [ ] 97 (beta.5.1, TOP): first setup let the admin password field get cleared/changed and created an account the owner could not log in with (fresh machine, 2026-09-10 ~01:30Z). Check: password confirm field, clear-on-blur/refocus behaviour, what the recovery kit does when the password is unknown, and make the readme say 'write the password down before Create'. Owner says: do not count his fresh-machine run as evidence.
- [ ] 98 (beta.5.1): README-START-HERE / QUICKSTART give no first-setup walkthrough and do not say where System Health is; owner could not get past setup card 2. Write a first-run guide with a screenshot per card.
- [ ] 99 (beta.5.1, HIGH): ProvisionJournal model forbids unknown fields; August (beta.1) journals carry 5 NATS context keys -> upgrade halts 'corrupt/unparseable' (measured on the Blackwell box 2026-09-10 02:49Z). Gate A xver lane over beta.4 cannot catch it. Fix PR launched (fix/provision-journal-tolerant-of-legacy-fields). Workaround: stop service, rename provision-journal.json -> .legacy, rerun setup.
- [ ] 100 (beta.5.1, HIGH): installer exit 85/127 'could not determine which CivicCast runtime owns this machine' on a box with uninstall history (ActiveRuntime deleted by post-uninstall; elevated HKEY_USERS ARP probe returns Unknown, reason not persisted). Runs AFTER provisioning mutated pg config. Fix PR launched (fix/runtime-ownership-claim-diagnosable): persist per-hive observation, gather HKLM ARP + Lxss + LxssManager evidence before refusing, claim before provisioning, 1060 = contained. Workaround: New-ItemProperty HKLM:\SOFTWARE\CivicCast ActiveRuntime=native, rerun setup. Report by Claude on the Blackwell box 2026-09-10 03:20Z.
- [ ] 101 (beta.6): run_native_provision (native_service_registration.rs:1999) captures the Python provisioning CLI's stderr and drops it, so no `provision note:`/`provision outcome:` line ever reaches install-progress.log. Forward the child's stderr (the handoff/password rides stdout, keep that boundary) -- fold into the runtime-ownership PR which already rewires that function.

## Clean-machine walkthrough 2026-09-09 (tester report, build 148c8d21 = beta.5) -- beta.6 candidates
- [ ] 102 (HIGH): recovery-kit one-time view vanishes within seconds, before Save/Print; next page says it was never confirmed. Must stay until explicit confirmation. (shot 05)
- [ ] 103 (HIGH): no current-session logout control; login routes redirect to First Setup. Add Sign out; make /login reusable after setup. (shot 83)
- [ ] 104 (HIGH, broadcast): resident portal Offline while the channel is On air (slate and asset); advertised /api/public/channels/public/live.m3u8 -> 404. Egress preset used was UDP SPTS to 127.0.0.1:5000. Determine whether the portal/HLS output is a separate sink that must be enabled by default, or a bug. (shots 124-125, 134)
- [ ] 105 (HIGH, broadcast): at the 22:00 schedule boundary the outgoing feed STOPPED instead of switching slate -> committed asset; manual Start showed Stopped twice then started ~24 s later. (shots 128-133)
- [ ] 106 (HIGH): startup after install: ERR_CONNECTION_RESET for ~7 min with no progress page. Serve a 'station starting' page or make the installer wait for /api/health. (shot 00)
- [ ] 107 (MED): Channels shows On air while Readiness says Idle / Do not broadcast yet / nothing on air. One authoritative state. (shot 127)
- [ ] 108 (MED): Start without an active egress silently returns to Stopped; needs an actionable error. Control Room Setup 'local-only output saved' vs Channels needing a second preset = two sources of truth. (shots 123-124)
- [ ] 109 (MED): bundled 'sample live source' says Ready in Setup/Readiness but fails its own RTMP probe and calls itself a legacy placeholder. Remove or make it pass. (shots 119-120)
- [ ] 110 (MED): channel elapsed time frozen (1m for 3+ min; 16 s for the asset). (shots 127, 133)
- [ ] 111 (MED): resident Schedule empty while resident Home shows the upcoming item. (shots 121-122)
- [ ] 112 (LOW): station name 'Test Station' not propagated to shell, Channels, resident portal. (shots 09, 64, 116)
- [ ] 113 (LOW): Now/next copy 'Queued to air aired'; generic sample playout plan shows 'Playing' while feed Stopped; 'Commit programs to air' wording; Cable Commissioning starts at 'Screen 8'; Reports empty export gives no feedback; role summary 4 vs badge 5.
- [ ] 114 (LOW): mobile 390x844 layouts (First Setup, Assets, Channels, Readiness) narrow/obscured.
- [ ] 115 (DOCS): README-START-HERE 4/5, QUICKSTART 3/5; First Setup copy fixes per the 'Confusing' table (recovery-kit location field looks like a path; double 'Optional'; 'Safe to broadcast' heading while verdict is Do not broadcast).
Report + 138 screenshots: Desktop\9.9.26 civiccast TESTresults-20260910T041852Z-1-001.zip (extracted under the session scratchpad tester-clean\).
- [ ] 97 UPDATE (from the clean-machine walkthrough): the password fields did NOT clear themselves; Chrome AUTOFILL painted an old username + masked password into the First Setup and recovery fields. Likely cause of the owner's lost password. Fix: autocomplete="new-password"/"off" + name attributes that autofill will not match on the First Setup admin form; re-test in a fresh Chrome profile.
- [ ] 116 (DOCS, HIGH for the helper prompt): shipped screen names differ from the docs/prompts: System Health = Readiness; Channel Ops = Channels; Live Room = Live; Facility Router = Facility; Contribute = Contributors; CG Board Designer = CG Designer; EAS = Emergency Alerts; Publish Dashboard = Publish; ActivityPub = Federation; Setup is split across First Setup / Control Room Setup / Cable Commissioning. Fix INSTALL-HELPER-PROMPT.md, README-START-HERE, QUICKSTART, USER-MANUAL cross-references.
- [ ] 117 (UX copy): First Setup 'Durable storage' card next-step says 'open the operator console' while the operator is already in it; role summary lists 4 defaults incl. Viewer but the badge shows 5 operator roles without Viewer.
- [ ] 118 (UX): Backup destination and Local archive folder fields have no Browse button; service-account permissions not foregrounded.
- [ ] 119 (UX guidance, 2/5 screens): AI Models, Paywall, Live, Control Room, Channels, CG Designer (vs CG Board), Auto-schedule, Playback Policy, App Admin, Readiness -- each needs plainer purpose text and one clear next action for a novice.
- [x] (info) Walkthrough: no red console errors on any screen; Cable Commissioning checks all passed except backup warning; lazy-load fallbacks settle; keyboard skip-link works; dark theme toggles.

## Upgrade-machine walkthrough 2026-09-09 (NvideaBlackwell, 5070 Ti; report + findings.json) -- beta.6 candidates
- [ ] 120 (CRITICAL, security): GET /api/setup/storage returns the live PostgreSQL URL with password to unauthenticated callers (F-01). Strip the credential; require the staff token on /api/setup/* once setup is complete. Rotate civiccast_svc on that station after the fix. F-03: /api/setup/station-state exposes admin_username, recovery_kit_id, storage locations unauthenticated. F-06: /openapi.json and /docs public (gate or local-only flag).
- [ ] 121 (MAJOR, safety): Publish pre-selects ALL surfaces (Portal, IA, NAS rsync/ZFS, YouTube Live/VOD, cable package); default must be Portal only (F-23). Also: recorded rehearsal items say 'Run the packager' but Assets offers no Package action for Recorded items.
- [ ] 122 (MAJOR): Channels shows fabricated Now/next, Playout plan, Proof log rows ('Playing', 'Captions Attached') on a stopped channel (F-27; = walkthrough-1 item 113). Render the empty state; never seed sample contracts into operator views.
- [ ] 123 (MAJOR): 'Start feed' accepted (202) and silently does nothing when no egress config exists; state endpoint returns null (F-29; = item 108). Disable Start with the inline reason; surface worker rejection as an alert.
- [ ] 124 (MAJOR): readiness headline 'Do not broadcast yet ... rehearsal is blocked' while its details say the rehearsal ran and finalized (F-21). Separate rehearsal result from broadcast gate; name and link the blocking item.
- [ ] 125 (MAJOR): Live screen has zero controls and its copy points at controls it lacks ('Choose Camera in Run Meeting') (F-19); manual step 4 names 'Run private rehearsal' which does not exist -- the control is 'Check broadcast readiness' (F-14).
- [ ] 126 (MAJOR): operator screens tell operators to run CLI commands / set env vars (System Health certs, IA/YouTube/NAS/webhook, Station Profile, Auto-schedule, Media Lifecycle, Publish readiness) (F-12); internal spec refs (S1, S3, S15, MASTER 13.1, tier-1, gemma4:12b) leak into copy (F-13).
- [ ] 127 (MAJOR): scheduled premiere never appears on the resident portal; 'scheduled' vs 'Publish to residents' are two undocumented steps (F-33; = item 111). Premiere of an already-public asset accepted and labelled not visible (F-32). Header 'Next: No events scheduled' ignores the new premiere (F-34).
- [ ] 128 (MAJOR): media/recordings/backups resolve under C:\Windows\system32\config\systemprofile\AppData\Local\CivicCast (LocalSystem %LOCALAPPDATA%); installer dialogs and QUICKSTART say C:\ProgramData\CivicCast (F-02). Decide: move under ProgramData\CivicCast\data or fix every doc/dialog; add Open folder.
- [ ] 129 (MAJOR, responsive): Assets and Channels tables overflow the page horizontally at ~710 px; schedule rows truncate titles to 'C...' (F-22, F-31; = item 114).
- [ ] 130 (sign-in UX): recovery submit is a two-click confirm whose first-click notice looks like an error and the button label does not change (F-08); username field accepts/echoes a password and gives no hint which value is the username (F-09); no code-format hint (CC- prefix) and no current-kit id shown (F-10); post-recovery lands on Federation instead of System Health/Setup (F-11). 16 minutes and three false failures for the owner with the correct kit in hand.
- [ ] 131 (MINOR): signed-out console polls staff endpoints in a tight loop (source-setup/backup/provider-readiness x4 in seconds; /api/staff/auth/me 39x in 15 min; sample-seed-status 141x in 10 min) (F-04, F-20). Clicking sidebar items signed-out silently lands on First setup (F-05). Sidebar items are buttons not links (F-07).
- [ ] 132 (MINOR): theme choice not persisted (F-28); portal Recordings list stale on in-app navigation after publish (F-24); portal posts analytics events while console says telemetry is off (F-25, needs white-box check); every readiness check creates an orphan rehearsal asset (F-26); rollback placeholder names 1.4.0 (F-15); GStreamer repair button ungated on main health page (F-16); cable headend defaults to 'Comcast MTD - HD' with a placeholder multicast (F-30); two 'default channel' settings disagree (government vs Public) (F-35); idle-page 'View published recordings' links to '/' (F-18); contradictory next-broadcast messaging on the resident home (F-17).
- [ ] 133 (INFO/PASS): documented first workflow steps 3-8 PASSED on the upgrade machine: sample media, backup verify, package, privacy check, Portal-only publish, resident HLS playback (color bars). Alerts ack/auto-resolve, daily self-check, support bundle redaction (no password/token), keyboard focus, dark theme all PASSED. Station box profile YELLOW only for NTP status; RTX 5070 Ti not shown though 'GPU captioning active'.
- [ ] 134 (OPS): the Blackwell station still carries the old WSL-era 'CivicCast Installer' 3.0.0-beta1 per-user ARP registration (the exit-85 cause); ActiveRuntime=native set by hand; August journal kept as .legacy.json; admin password changed via recovery (one code consumed); a premiere scheduled for 11:00 PM 09-09.
Reports: Downloads\CivicCast-beta5-walkthrough-handoff-2026-09-09.zip (extracted under the session scratchpad tester-upgrade\).
- [ ] 100 UPDATE (corrected field report): the exit-85 cause on the Blackwell box was a PRESENT per-user ARP entry for the WSL-era 'CivicCast Installer 3.0.0-beta1' (inert: no CivicCast distro, no autostart, no service), not an Unknown probe error; #209 round 3 adds a PresentInert->ClaimNative rule with a warning and a product-naming refusal text. Also: D3 upgrade engine routed the August install as FRESH_INSTALL (old=none) despite InstalledVersion 1.0.0-beta.1 recorded on 08-15 -- investigate (item 135).
- [ ] 135 (MED): D3 upgrade engine classifies a machine with an August native install + preserved ProgramData as FRESH_INSTALL (install-progress.log 2026-09-09 20:49 and 21:03 'd3-engine: begin (old=none)'); preinstall on run 2 said 'existing install found' yet D3 said old=none. Find which identity D3 reads.
- [ ] 107 UPDATE: screenshot 127 shows the Readiness 'ON AIR RIGHT NOW' card computes 'Idle' from auto_start (24/7 automation) channels only ('No channels are configured for 24/7 automation') while a manually started channel is On air -- the summary must count manual on-air channels too.
