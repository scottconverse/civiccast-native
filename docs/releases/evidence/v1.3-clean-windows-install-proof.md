# v1.3.0 clean Windows install proof

Status: `passed`
Release gate: `closed for v1.3.0 private beta`
Dry run: `false`
VM booted: `true`
Release manifest: `artifacts\release\v1.3.0\civiccast-1.3.0-release-artifacts-manifest.json`

Final result: the MSI Windows proof machine completed installer-to-dashboard
clean proof from the v1.3.0 Windows setup executable. The setup app guided WSL2
Ubuntu 24.04 preparation, started CivicCast, opened the operator console,
created the first admin, saved recovery-kit evidence, verified backup, passed
restore rehearsal, ran private rehearsal, confirmed resident preview, and
created a redacted support bundle. The final passing report is
`tester-handoff/v1.3.0/test-results/windows/20260528-151015-msi-b0f532f.md`.

The earlier VirtualBox lane below remains recorded as historical evidence for
the nested-virtualization blocker on that host.

## Artifact Under Test

- File: `civiccast-1.3.0-windows-setup.exe`
- SHA-256: `392c4960d42eb915dc0fd25180ec5ae07e7a72f2283ddcddad664843677ead1c`
- Size: `168364263` bytes

## Isolated Target

- Engine: VirtualBox 7.2.8
- VM: `civiccast-v13-cleanwin`
- OS: Windows 11 Enterprise Evaluation 10.0.26200
- ISO: Microsoft Windows 11 Enterprise Evaluation 25H2

## Attempts

### virtualbox-clean-windows-installer

- Status: `blocked`
- VM booted: `true`
- Evidence:
  - Booted a Windows 11 Enterprise Evaluation VM.
  - Copied the rebuilt v1.3.0 Windows setup executable into the guest.
  - Installed the NSIS package silently inside the guest.
  - Launched the CivicCast Installer UI.
  - Started the WSL2 Ubuntu bootstrap from the installer UI.
  - Approved the Microsoft PowerShell elevation prompt.
  - Installer enabled Windows WSL components and saved a reboot-required resume
    state.
  - Rebooted the VM and reopened the CivicCast Installer.
  - Reran the WSL2 Ubuntu bootstrap from the installer UI.
  - Retested the final rebuilt setup executable after WSL bootstrap parser
    hardening.
  - Installer displayed an actionable virtualization/IT-help blocker instead of
    a generic missing-WSL loop.

Final artifact retest:

- Mounted payload ISO:
  `C:\Dev\Claude\vm-proof\civiccast-v13-cleanwin-final\civiccast-final-installer.iso`
- Payload SHA-256:
  `392c4960d42eb915dc0fd25180ec5ae07e7a72f2283ddcddad664843677ead1c`
- Screenshot:
  `C:\Dev\Claude\vm-proof\civiccast-v13-cleanwin\after-runfinal-45s.png`

Blocker evidence:

```text
WSL2 is unable to start since virtualization is not enabled on this machine.
Error code: Wsl/InstallDistro/Service/RegisterDistro/CreateVm/HCS/HCS_E_HYPERV_NOT_INSTALLED
```

Operator-visible result:

```text
Windows installed WSL components, but this machine does not expose virtualization
for WSL2. Turn on virtualization or ask IT for help, then rerun CivicCast Installer.
```

### wsl2-fresh-user

- Status: `passed`
- VM booted: `false`
- Evidence: the rebuilt wheelhouse installed in a fresh temporary WSL2 user venv
  and imported CivicCast 1.3.0.

## Release Gate

The v1.3.0 private beta release was tagged and published after the MSI
clean-machine Windows proof passed. v1.3.1 must still run its own smoke proof
from the published GitHub Release `.exe` asset before it becomes the preferred
tester build.
