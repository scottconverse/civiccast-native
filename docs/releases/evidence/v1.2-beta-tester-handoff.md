# v1.2 beta tester handoff evidence

Status: `passed with gated lanes`. Package acquisition and WSL2 fresh-user clean
install proof passed locally for the v1.2.0 release candidate; dependency,
model, NATS, mTLS, and external-provider lanes remain fail-closed until each
carries its own evidence.

The beta handoff route is `/api/staff/installer/beta-handoff`, and the matching
CLI command is:

```powershell
.\.venv\Scripts\civiccast.exe installer beta-handoff --json
```

Current evidence language is fail-closed:

- Package acquisition passed for the local release-candidate manifest at
  `artifacts\release\civiccast-1.2.0-release-artifacts-manifest.json`; it
  contains `beta_handoff_acquisition` with artifact filenames, hashes, and the
  offline install command.
- Clean Windows install proof passed through the WSL2 fresh-user lane. Hyper-V
  and Windows Sandbox remain recorded blockers on this host. A follow-up
  elevated native probe and controlled `-NoRestart` enable attempt proved the
  RTX validation host is Windows 11 Home and does not expose the required
  `Microsoft-Hyper-V-All` or `Containers-DisposableClientVM` feature names. The
  proof runner still performed a real offline wheelhouse install into a
  disposable WSL2 venv and imported CivicCast `1.2.0`. A VirtualBox follow-up
  booted a fresh Windows 11 Enterprise Evaluation VM, installed and launched the
  packaged installer, found the WSL2-missing path was too manual, then verified
  the rebuilt installer exposes `Install WSL2 Ubuntu`, opens a Microsoft WSL
  UAC prompt, and launches WSL installation. Full WSL2 runtime completion
  inside the VM is still blocked by nested virtualization.
- External provider lanes remain `credential_or_secret_required` without
  approved credentials, controlled targets, and redacted live proof.
- FFmpeg, NDI, Ollama, and model setup are detection or operator-gated
  acquisition lanes. CivicCast does not redistribute restricted third-party
  installers or model weights as proof.
