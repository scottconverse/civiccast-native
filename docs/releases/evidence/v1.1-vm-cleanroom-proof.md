# v1.1 VM Cleanroom Proof

Date: 2026-05-17

Environment: WSL2 Ubuntu VM on `TinyRyzen7`

Install source:

`artifacts/release-candidate/civiccast-1.1.0-py3-none-any.whl`

Commands:

```powershell
wsl -d Ubuntu -- bash -lc "python3.12 -m venv /tmp/civiccast-v11-cleanroom-base/venv"
wsl -d Ubuntu -- bash -lc "/tmp/civiccast-v11-cleanroom-base/venv/bin/python -m pip install /mnt/c/Users/scott/OneDrive/Desktop/Claude/CivicCast/artifacts/release-candidate/civiccast-1.1.0-py3-none-any.whl"
wsl -d Ubuntu -- /tmp/civiccast-v11-cleanroom-base/venv/bin/civiccast --version
wsl -d Ubuntu -- /tmp/civiccast-v11-cleanroom-base/venv/bin/civiccast doctor --json
wsl -d Ubuntu -- /tmp/civiccast-v11-cleanroom-base/venv/bin/python -c "import civiccast.app; print(civiccast.app.app.title)"
```

Results:

```text
civiccast 1.1.0
recommended_tier: tier-0
os.kind: wsl2
app title: CivicCast
```

The cleanroom VM installed from the built wheel artifact, imported the FastAPI
app, and ran the operator hardware probe. This proof does not replace the
self-hosted RTX AI lane; it verifies the installable package on an isolated
Linux VM.
