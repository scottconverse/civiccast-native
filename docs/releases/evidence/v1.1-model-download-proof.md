# v1.1 Model Download Proof

Date: 2026-05-17

Command:

```powershell
.\.venv\Scripts\python.exe -m civiccast.cli model download --cache-dir .civiccast-local\models --json
```

Result: PASS.

Runtime proof:

| Model | Runtime | Source | Local status |
| --- | --- | --- | --- |
| `whisper-large-v3` | faster-whisper | `Systran/faster-whisper-large-v3` | Downloaded to `.civiccast-local\models\faster-whisper-large-v3`. |
| `gemma4:e4b` | Ollama | `ollama pull gemma4:e4b` | Pulled and SHA-256 digest verified by Ollama. |
| `translategemma:4b` | Ollama | `ollama pull translategemma:4b` | Pulled and SHA-256 digest verified by Ollama. |

The model cache path is intentionally gitignored. The release proof records that
the installer-facing command can pull the three real local AI models on a fresh
host; release artifacts do not commit model weights.
