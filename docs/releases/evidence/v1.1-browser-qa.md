# v1.1 Browser QA Evidence

Current executor status: live browser proof completed for the current branch
state on 2026-05-17.

## Operator Portal

Command:

```powershell
cd civiccast\apps\portal-operator
npm run build
npm run test:a11y -- --grep "a11y|summary|caption|schedule|live|publish"
```

Result:

```text
portal-operator build: PASS
operator Playwright: 49 passed
```

Coverage:

- Desktop and mobile accessibility shell checks.
- Loading, success, empty, error, and partial states across caption review,
  live room, publish dashboard, schedule, signed records, and summary review.
- Keyboard/focus flows for navigation, drawer focus trap, schedule cards, source
  switching, and transcript seek controls.
- Browser console was checked during the Playwright runs; no release-blocking
  console errors were observed on the covered operator surfaces.
- Zero serious or critical axe violations on the covered operator surfaces.
- Error copy sweep verified by `scripts/policy/check_operator_copy.py`.

## Public Portal

Command:

```powershell
cd civiccast\apps\portal-public
npm run build
npm run test:a11y
```

Result:

```text
portal-public build: PASS
public Playwright: 14 passed
```

Coverage:

- Resident loading, success, empty, partial, idle, emergency overlay, invalid
  token, confirmed token, unsubscribe, podcast link, and mobile subscription
  surfaces.
- Browser console was checked during the Playwright runs; no release-blocking
  console errors were observed on the covered public surfaces.
- Zero serious or critical axe violations on the covered public surfaces.
