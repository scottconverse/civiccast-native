# v1.2 New-Box Handoff Validation

Date: 2026-05-20

Status: passed for package integrity, git bundle integrity, installer GUI
launch, and package-local NDI receiver source discovery.

Run: `2026-05-20-v1.2-proof-consolidation`

## Source Package

The old-box handoff package was transferred through the private bridge repo as
`complete-new-box-handoff-2026-05-20.tar.gz.part001` and extracted on new-box
under a short path to avoid Windows path-length loss:

```text
C:\cc20\2026-05-20T22-35-00Z_complete-new-box-handoff-package
```

Outer archive SHA-256 matched the directive:

```text
1C74FD2727B8B1E0080682303C2CF298824A29899B4F421911381A472BF51674
```

The package's internal `SHA256SUMS.json` check verified `551` entries with
zero failures.

## Git Bundle

The CivicCast bundle verified as a complete-history bundle:

```text
repo-bundles\CivicCast-all-refs.bundle
```

The bundle advertises `46` refs and includes `main` at:

```text
e14987abc4954c903e96515ed473b07063360746
```

## Windows Installer GUI Launch

Packaged installer:

```text
artifacts\windows-installer\civiccast-1.1.1-windows-setup.exe
```

Observed result:

```text
window_title=CivicCast Installer Setup
process_responding=true
closed_cleanly=true
install_performed=false
```

This proves package-local GUI launch only. It does not prove a clean-machine
Windows installation.

## NDI Receiver Source Discovery

Packaged Studio Monitor:

```text
artifacts\ndi-studio-monitor\Studio Monitor\Application.Network.StudioMonitor.x64.exe
```

Packaged sender:

```text
artifacts\ndi-sender-release\civiccast-ndi-ffmpeg-sender.exe
```

The sender was launched with process-local PATH changes only, including the
package runtime and the local FFmpeg Essentials binary directory. No persistent
global environment change was required for this proof.

Sender output:

```text
runtime=ffmpeg-rawvideo-to-ndi name="CivicCast Lab Proof" width=1280 height=720 fps=30/1
ndi_sender_result=ok frames_sent=900
```

Studio Monitor launched, responded, and exposed its local API on port `80`.
The local API response from `GET http://127.0.0.1:80/v1/sources` contained:

```text
NVIDEABLACKWELL (CivicCast Lab Proof)
```

This proves package-local NDI source discovery by a real NDI receiver on
new-box. It does not prove NDI output from a CivicCast media package, a public
redistributable FFmpeg+NDI muxer, SDI/DeckLink output, live headend delivery,
or real station cable proof.

## Cleanup

Studio Monitor, the sender, installer launch process, and FFmpeg child
processes were closed after validation. A process check found no leftover
installer, NDI sender, Studio Monitor, or FFmpeg processes from the proof.

## Secret Candidate Review

The handoff package included a value-safe secret/config candidate review. One
old-box filtered environment file contained a GitHub-token-shaped value. The
raw value was not printed, migrated, or committed. Scott should manually review
and rotate/migrate any credential material as needed outside this evidence
file.

## Boundary

Passed:

- Handoff archive integrity.
- Internal package checksum verification.
- CivicCast git bundle integrity.
- Packaged installer GUI launch.
- Package-local NDI receiver source discovery.

Still not claimed:

- Clean-machine Windows installation from the setup executable.
- Public redistributable FFmpeg+NDI binary.
- CivicCast media-package-to-NDI receiver proof.
- SDI/DeckLink output.
- Live cable headend delivery.
- FCC Part 79 field certification.
- Real station proof.
- External provider proof.
