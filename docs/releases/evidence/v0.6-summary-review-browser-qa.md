# v0.6 Summary Review Browser QA Evidence

Run: `2026-05-14-v0.6-summary-signed-records`
Date: 2026-05-14

## Browser Coverage

Command:

```powershell
npx playwright test e2e/summary-review.spec.ts e2e/signed-records.spec.ts
```

Result:

```text
9 passed
```

## Evidence Files

- `docs/releases/evidence/v0.6-summary-review-success-desktop.png`
- `docs/releases/evidence/v0.6-summary-review-success-mobile.png`
- `docs/releases/evidence/v0.6-summary-review-partial-mobile.png`
- `docs/releases/evidence/v0.6-signed-record-export-desktop.png`

## States Checked

- Loading: skeleton rows render while summary review data is delayed.
- Success: sourced summary with timestamp-backed quantitative claim renders.
- Empty: tells the operator to generate a summary from committed transcript cues.
- Error: tells the operator to retry and check API logs or `DATABASE_URL`.
- Partial/refusal: tells the operator to regenerate after adding timestamp evidence.
- Signed-record success: verified record id and timestamp digest render after export.
- Signed-record error: export error includes an approval-first recovery step.

## UX Checks

- Desktop sourced-claim link seeks `cue-42` to `3:08-3:25`.
- Mobile sourced-claim link seeks the same cue.
- Focus preservation: activating the sourced-claim link returns focus to
  **Approve summary**.
- Keyboard/focus: Playwright focuses the action button before seek and asserts it
  remains focused after seek.
- Copy: every empty/error/partial/export failure message includes a concrete next
  step.
- Browser console: success flows assert no `console.error` messages.
- Accessibility: axe-core scan reports no serious or critical violations.
