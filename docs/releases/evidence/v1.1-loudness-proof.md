# v1.1 Loudness Proof

Date: 2026-05-17

Gate: ITU-R BS.1770 / EBU R128 integrated loudness.

Local proof command:

```powershell
ffmpeg -f lavfi -i sine=frequency=1000:duration=10 -af volume=0.1 .agent-runs\2026-05-17-v1.1-public-availability\runtime-proof\loudness-proof.wav
.\.venv\Scripts\python.exe - <<'PY'
from pathlib import Path
from civiccast.stream.loudness import check_streaming_loudness
print(check_streaming_loudness(Path(".agent-runs/2026-05-17-v1.1-public-availability/runtime-proof/loudness-proof.wav")))
PY
```

Result:

```text
LoudnessGateResult(status='ok', target_lufs=-16.0, tolerance_lufs=1.0, measured_lufs=-16.0, operator_action='Audio loudness is within the v1.1 release tolerance.')
```

The code path parses real ffmpeg `ebur128` output; it no longer substitutes the
target value as the measured value.
