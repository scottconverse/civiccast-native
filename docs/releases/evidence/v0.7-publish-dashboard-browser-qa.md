# v0.7 Publish Dashboard Browser QA Evidence

Run: `2026-05-14-v0.7-three-tier-publish`
Date: 2026-05-14

## Browser Coverage

Command:

```powershell
npx playwright test e2e/publish-dashboard.spec.ts
```

Result:

```text
7 passed
```

## Evidence Files

- `docs/releases/evidence/v0.7-publish-dashboard-desktop.png`
- `docs/releases/evidence/v0.7-publish-dashboard-mobile.png`

## States Checked

- Loading: skeleton rows render while publish dashboard data loads.
- Success: portal, Internet Archive, Local NAS rsync, Local NAS ZFS, YouTube
  Live, and YouTube VOD render as independent surfaces.
- Error: database-unavailable state tells the operator to start the API with
  `DATABASE_URL` set, then retry.
- Partial/degraded: failed Internet Archive and YouTube Live rows stay visible
  with platform-specific next steps and retry controls.
- Override: required archive override is disabled until a specific
  audit-logged justification is entered.
- Retry: retrying a failed surface sends only that asset/surface request and
  preserves unrelated targets.

## UX Checks

- Desktop viewport evidence captured at 1440 x 1100.
- Mobile viewport evidence captured at 390 x 1100 through the mobile
  navigation drawer.
- Browser console: screenshot capture reported `console_errors=0` on mobile;
  Playwright interaction suite asserts publish workflows complete without page
  errors.
- Accessibility: axe-core scan reports no serious or critical violations.
- Copy: preflight, archive override, retry, failed credential, database error,
  and empty states include concrete next steps.
