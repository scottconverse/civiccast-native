# CivicCast 3.1 Control Room LPM Local Verification - 2026-06-29

This is a local-only verification note for the 3.1 LPM control-room planning and runner scaffold on branch `local/3.1-control-room-lpm`.

No GitHub branch was pushed. No PR was opened. No merge was performed. No tag or release was created.

## What Changed

The local branch now carries the 3.1 LPM control-room studio-proof spec, the workstream document, the TSR ADR backfill draft, the caption workstream draft, and a local CI runner for this 3.1 surface. The runner is intentionally local-only and includes a self-check that rejects publish-oriented commands inside the runner body.

The spec records that any mocked, simulated, or lab-only proof must be documented
as such and must stay out of station-device release claims until it is proven
against the real device path.

## Local Runner

Command:

```powershell
powershell -ExecutionPolicy Bypass -File scripts\run_local_3_1_control_room_ci.ps1
```

The runner writes reviewable logs under `artifacts/local-ci/3.1-control-room-<timestamp>/`, including `transcript.txt`, one numbered log per step, `git-status.txt`, and `summary.json`.

## Verification Runs

Full local implementation runner, after the 2026-06-30 mode-gate and material
state work:

```powershell
powershell -ExecutionPolicy Bypass -File scripts\run_local_3_1_control_room_ci.ps1
```

Result: passed.

Evidence:

```text
Python control-room tests: 57 passed
Python policy slice: 6 passed, 1 skipped
OpenAPI artifact check: passed
portal-operator ControlRoom/client unit tests: 3 files passed, 19 tests passed
```

The runner also completed its local-only self-check: it did not push, merge,
tag, open a PR, or create a release. The skipped policy test still requires
`CIVICAST_POSTGRES_TEST_URL` for an external Postgres lifecycle check; that is
unchanged environment scope, not a new 3.1 control-room failure.

Artifact root:

```text
C:\CivicCastTester\v3-beta-release-prep\artifacts\local-ci\3.1-control-room-20260630-033414
```

Additional focused checks run outside the runner:

```text
uv run python -m pytest -q tests/control_room
Result: 57 passed

uv run python scripts/generate-openapi-artifacts.py --check
Result: passed

npm.cmd --prefix civiccast/apps/portal-operator run test:unit -- ControlRoom client.test
Result: 3 files passed, 19 tests passed

npm.cmd --prefix civiccast/apps/portal-operator run build
Result: passed; Vite emitted only the existing large-chunk warning
```

Docs-only/self-check run:

```powershell
powershell -ExecutionPolicy Bypass -File scripts\run_local_3_1_control_room_ci.ps1 -DocsOnly
```

Result: passed.

Artifact root:

```text
C:\CivicCastTester\v3-beta-release-prep\artifacts\local-ci\3.1-control-room-20260629-231658
```

Python/OpenAPI run:

```powershell
powershell -ExecutionPolicy Bypass -File scripts\run_local_3_1_control_room_ci.ps1 -SkipWeb
```

Result: passed.

Evidence:

```text
Python control-room tests: 43 passed
Python policy slice: 6 passed, 1 skipped
OpenAPI artifact check: passed
```

The skipped policy test requires `CIVICAST_POSTGRES_TEST_URL` for an external Postgres lifecycle check. That skip is existing environment scope, not a new 3.1 control-room failure.

Artifact root:

```text
C:\CivicCastTester\v3-beta-release-prep\artifacts\local-ci\3.1-control-room-20260629-231738
```

Operator portal ControlRoom run:

```powershell
powershell -ExecutionPolicy Bypass -File scripts\run_local_3_1_control_room_ci.ps1 -SkipPython -SkipOpenApi
```

Result: passed.

Evidence:

```text
portal-operator ControlRoom unit tests: 2 files passed, 15 tests passed
```

Artifact root:

```text
C:\CivicCastTester\v3-beta-release-prep\artifacts\local-ci\3.1-control-room-20260629-231756
```

## Not Station-Device-Proven Yet

The local runner does not claim live OBS, live vMix, DeckLink-through-vMix,
NDI-through-vMix, ATEM-upstream-to-vMix, or PTZ state as station-device-proven.
Those evidence checks are intentionally separate opt-in live checks and must be
run against LPM's equipment before release notes or beta-facing docs can claim
them.

The runner currently fails intentionally if `-IncludeLiveObs` or `-IncludeLiveVmix` is passed before those adapter proof commands exist.

## Exit Status

The local 3.1 control-room/LPM track now has a first implemented safety slice:
network/payload policy, material-state Dry Run fingerprinting, Test Mode
server-side non-dispatch, On-Air confirmation plus safe-state requirement,
On-Air expiry, API/OpenAPI/types updates, and operator-console affordances for
Test Mode vs On-Air Mode.

This is still local-only. It is not pushed, merged, tagged, released, or claimed
as station-device-proven.
