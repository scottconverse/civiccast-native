# CivicCast 3.0.1 Worklist

Date: 2026-06-25

This file tracks post-beta1 follow-up work that is not allowed to blur the
v3.0.0-beta1 release evidence.

## Health/schema diagnostics

- Source fix landed: packaged/runtime schema-health detection now resolves
  Alembic script and migration-version paths from the installed `civiccast`
  package layout instead of the process working directory.
- Remaining closure proof: build a new packaged wheel or installer runtime and
  exercise `/health` from that installed package, proving schema status is not
  `unknown`.
- Root cause observed during the R19 cleanroom proof: the packaged installer
  runtime can serve beta tester workflows successfully while the health check
  cannot locate the packaged Alembic metadata path.
- Regression added: `expected_migration_head()` is now tested from a non-repo
  working directory so cwd-relative Alembic lookup cannot return.
- Beta1 impact: diagnostic/status only. R19 install, dashboard, first-admin,
  saved-state, and scheduled-recording proof passed.
