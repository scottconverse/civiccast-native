# v1.2 Spec Alignment Ledger

Run: `2026-05-19-v1.2-airgap-vm-proof`

Beta handoff update: `2026-05-21-beta-tester-handoff`

## Status Vocabulary

- `Implemented`: code, tests, and release evidence exist in this run.
- `Implemented, credential-gated`: code path exists and stops with
  `credential_or_secret_required` until Scott supplies real credentials.
- `Implemented, hardware-gated`: code path exists and stops with the documented
  hardware status until the required machine is available.
- `Implemented, fresh-user proof`: code, tests, and evidence exist, and clean
  install proof passed in a disposable WSL2 fresh-user environment while
  unavailable Hyper-V or Windows Sandbox lanes remain recorded as host-gated.
- `Deferred by Scott`: excluded from this internal v1.2 slice by directive.

| item | status | evidence |
| --- | --- | --- |
| BOOT-001 offline model bundle hashes | Implemented | `civiccast model download --offline-bundle --bundle-dir ...` and `verify_airgapped_install()` use SHA-256 values from real files; missing files fail actionably. Evidence: `docs/releases/evidence/v1.2-first-run-gates.md`; tests: `tests/installer/test_v12_first_run_verification.py`. |
| TEST-001 active release run id | Implemented | `scripts/verify-release.sh` defaults to `2026-05-19-v1.2-airgap-vm-proof` and writes a11y receipts under `${RELEASE_RUN_ID}`. Tests: `tests/test_verify_release_run_id.py`. |
| DOC-001 internal handoff truth | Implemented for this branch handoff | `HANDOFF.md` remains internal/gitignored; this branch uses `.agent-runs/2026-05-19-v1.2-nats-mtls-foundation/` as the durable run authority. New-box handoff package integrity, git bundle integrity, installer GUI launch, and NDI Studio Monitor source discovery are recorded in `docs/releases/evidence/v1.2-new-box-handoff-validation.md`. |
| DOC-002 current-facing release copy | Implemented | README, changelog, docs index, user manual, API reference, credential matrix, and v1.2 evidence docs describe v1.2.0 as a public beta release with explicit non-claims for public fediverse compatibility, external-provider credentials, public redistributable FFmpeg+NDI, SDI/DeckLink, and live headend delivery. |
| SEC-001 staff token lifecycle | Implemented before this run | v1.2 staff-token lifecycle tests remain green in the full suite: `tests/auth/test_staff_token_lifecycle.py`. |
| ENG-001 router module-scope store singletons | Implemented | Router default store singletons were removed. Store ownership lives in `app.state.store_bundle`; evidence: `docs/releases/evidence/v1.2-store-isolation-proof.md`; tests: `tests/schedule/test_app_wiring.py`. |
| ENG-002 ADR 0001 broker substrate | Implemented | `civiccast.platform.broker` provides `BrokerClient`, an explicit dev/test-only `InProcessBrokerClient`, and a concrete `NATSJetStreamBrokerClient` selected fail-closed for production through `civiccast.platform.broker_config`; publish approval emits `publish.asset.approved` through the platform contract. Evidence: `docs/releases/evidence/v1.2-broker-contract.md` and `docs/releases/v1.2-nats-mtls-foundation.md`. |
| BOOT-002 first-run target verification | Implemented, credential-gated | First-run external/provider checks fail closed with actionable credential/path states instead of deterministic green proof; configured provider credentials remain blocked until live verification evidence exists, and local NAS returns `ok` only after a write/read/delete hash probe. Evidence: `docs/releases/evidence/v1.2-first-run-gates.md`. |
| RTX caption proof | Implemented | New-box RTX proof passed with real `faster-whisper` `whisper-large-v3` on CUDA, CTranslate2 `int8`, GPU/VRAM samples from the RTX 5070 Ti, approved synthetic fixture provenance, and WER `14.29%` against the release floor. Evidence: `docs/releases/evidence/v1.2-rtx-caption-proof.md`. |
| Air-gapped VM proof | Implemented | `scripts/build_release_artifacts.py --wheelhouse` builds a Linux CPython 3.12 offline dependency wheelhouse with SHA-256 manifest, and `scripts/run_airgap_vm_proof.py --execute-vm` verifies release artifacts, verifies the wheelhouse, removes the WSL2 VM default route, installs CivicCast from the local application wheel plus wheelhouse, imports the installed app, and verifies the three real model archives from bytes. Evidence: `docs/releases/evidence/v1.2-airgapped-vm-proof.md`; tests: `tests/integration/test_airgap_vm_proof.py` and `tests/installer/test_package_artifacts.py`. |
| External provider proofs | Implemented, credential-gated | IA, YouTube, subscriber notification, and other external proof lanes stop until credentials and live proof evidence are available; local NAS can only pass after the local hash probe succeeds. |
| ActivityPub federation | Implemented; local GoToSocial actor interop proven, public target-instance proof still operator-gated | `civiccast.activitypub` is disabled by default, but enabled deployments require explicit base URL and station key material, verify signed `Follow`/`Undo Follow` inbox traffic, sign outbound `Accept`/`Reject`/`Create` delivery, persist followers/outbox/delivery attempts, and expose open, limited, approval-only, blocklist, allowlist, authorized-fetch, operator console moderation, and installer setup controls. CI uses local signed actors and receivers instead of committing a public fediverse account. After Docker Desktop repair, the local GoToSocial proof runner passed at signed actor-document proof level against a disposable container; public target-instance `Follow`/`Accept` proof remains a separate credential/operator lane. Evidence: `docs/releases/evidence/v1.2-activitypub-federation.md`; tests: `tests/activitypub/`, `civiccast/apps/portal-operator/e2e/activitypub.spec.ts`, `civiccast/apps/installer/e2e/installer.spec.ts`. |
| Cable file package | Implemented, proof-deferred | `civiccast.cable` creates a local PEG/headend handoff ZIP from real media and caption sidecar files with `manifest.json`, `SHA256SUMS`, and package SHA-256 proof. Evidence: `docs/releases/evidence/v1.2-cable-file-package.md`; tests: `tests/cable/` and `tests/publish/test_cable_file_package_surface.py`. |
| NDI output planning/readiness | Implemented, internal lab proof and receiver discovery available | `civiccast.cable` builds a local-file-to-NDI FFmpeg command plan and checks host readiness through `civiccast cable ndi-plan` and `civiccast cable ndi-check`. The host FFmpeg build still lacks an NDI muxer, but `tools/ndi-ffmpeg-sender` launches FFmpeg, reads BGRA rawvideo, and publishes frames through the local NDI runtime for private station testing. New-box package-local Studio Monitor API proof shows the receiver discovered `NVIDEABLACKWELL (CivicCast Lab Proof)`. Public redistributable FFmpeg+NDI packaging, CivicCast media-package-to-NDI receiver proof, visual receiver capture, SDI/DeckLink, live headend delivery, and station proof remain deferred. Evidence: `docs/releases/evidence/v1.2-ndi-output.md` and `docs/releases/evidence/v1.2-new-box-handoff-validation.md`; tests: `tests/cable/test_ndi_output.py` and `tests/cable/test_cable_cli.py`. |
| Windows double-click installer and beta handoff | Implemented, fresh-user proof plus VirtualBox clean Windows installer proof | The Tauri installer walkthrough builds into a Windows NSIS setup executable through `npm run tauri:build`, and `scripts/build_release_artifacts.py --all-portable --python --wheelhouse --windows-installer` copies the real setup bytes into the release artifact directory with SHA-256 manifest metadata plus `beta_handoff_acquisition`. New-box package-local validation launched the packaged installer GUI and closed it cleanly. The clean Windows proof runner records Hyper-V and Windows Sandbox host blockers on this Windows Home RTX host, then passes the WSL2 fresh-user lane by installing CivicCast offline from the generated wheelhouse and wheel in a disposable WSL2 venv. A follow-up elevated native attempt installed VirtualBox, booted a fresh Windows 11 Enterprise Evaluation VM, installed and launched the packaged setup, exposed that the pre-fix WSL2-missing lane still required a manual `wsl --install -d Ubuntu`, then validated the rebuilt installer with an enabled `Install WSL2 Ubuntu` action that opened a Microsoft-verified Windows Subsystem for Linux UAC prompt and installed WSL `2.7.3`. The final command checks `wsl.exe -l -v` for Ubuntu before treating the lane as ready because the VM proof showed `wsl.exe --status` can pass without an installed distro. Full WSL2 Ubuntu runtime completion inside that VM remains blocked by nested virtualization, so the WSL2 fresh-user lane remains the runtime proof. Evidence: `docs/releases/evidence/v1.2-windows-installer-ndi-sender.md`, `docs/releases/evidence/v1.2-new-box-handoff-validation.md`, `docs/releases/evidence/v1.2-beta-tester-handoff.md`, `docs/releases/evidence/v1.2-clean-windows-install-proof.md`, `docs/releases/evidence/v1.2-native-windows-clean-install-attempt.md`, and `docs/releases/evidence/v1.2-virtualbox-clean-windows-proof.md`; tests: `tests/installer/test_package_artifacts.py`, `tests/installer/test_beta_handoff.py`, and `tests/integration/test_clean_windows_install_proof.py`. |
| Real station pilot / five-station adoption / seated governance | Deferred by Scott | Excluded from public-availability hardening and not claimed. |
| TSA/legal signing authority / production deployment proof | Deferred by Scott | Excluded from this run and not claimed. |
