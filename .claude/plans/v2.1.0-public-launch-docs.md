# v2.1.0 public-launch documentation pass

Goal: every version-bearing surface on v2.1.0, README complete, USER-MANUAL
(non-tech / tech / architecture) accurate + professional, landing page polished
and representing the full product range, fresh seed discussion posts, then flip
the repo public.

## Core content gap (the important one)

README, USER-MANUAL, and the scope docs describe v2.1.0's proof boundary as
only the egress lineage (v2.0.8 Windows path + v2.0.9 FileSink/SRT + E.2
adversarial headend). They predate this release's **beta sprint** — the seven
product decisions actually shipped in v2.1.0:

1. Live caption tap (egress audio fork → durable review queue, config-gated)
2. Honest retention copy (flag-for-review, never auto-purge)
3. Operator repair surfaces (finalization retry + ActivityPub dead-letter replay)
4. Real trim repackage-on-update (published recording re-renders)
5. Recording provenance stamp
6. Real provider adapters (Internet Archive, YouTube, SMTP), config-gated;
   mocks remain default
7. CDN publishing wiring (packaged recordings upload through selected CDN)

Plus the new evidence: a clean-machine double-click Windows install proof
(Hyper-V VM) reached `ready` with first-admin + recovery kit. Docs must say so.

## Work items

1. **Stale versions → 2.1.0**: CAPABILITIES.md (baseline line), ARCHITECTURE.md,
   docs/spec/spec.md (5), docs/tester/known-limitations.md (2),
   INSTALL-WINDOWS.md (3). Keep historical/audit/CHANGELOG mentions intact.
2. **Create** `docs/releases/v2.1.0-scope-and-evidence.md` (README + manual link
   it; currently missing) — accurate to the beta sprint + install proof.
3. **README**: refresh the proof-boundary paragraph and "What Ships in v2.1.0"
   to include the beta sprint capabilities + install proof; keep structure.
4. **USER-MANUAL**: same content refresh in the intro + technical + provider
   sections; keep the non-tech / operator / technical / architecture structure
   and Pandoc frontmatter. Regenerate PDF/DOCX via WSL pandoc.
5. **Landing page** `docs/index.html`: represent the full range (captions +
   translation, archive/reach providers, channel egress, federation, CDN),
   fix any stale/broken doc links, keep the professional layout + a11y.
6. **Seed posts**: create `docs/public/github-discussions/v2.1.0-community-seed-pack.md`
   + refresh the individual seed posts for the v2.1.0 beta.
7. **Secret scan** the whole tree (no tokens/keys/passwords/recovery codes),
   verify `gh secret`/workflow files carry no literals, then
   `gh repo edit --visibility public`. This is outward-facing + hard to fully
   reverse — scan is a hard gate; stop and report if anything sensitive surfaces.

## Discipline

check_release_identity stays green; markdown links resolve; landing-page a11y
(skip link, reduced-motion, ARIA) preserved; commit per logical group; PR to
main. The public flip is the final step after Scott can see the doc state.
