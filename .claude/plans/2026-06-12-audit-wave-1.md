# Audit wave 1 — Critical + daemon correctness (sprint punch list #1-#11)

> Source: audit-civiccast-cable-sprint-2026-06-12 (exec report + punchlist).
> Policy: zero at all severities; waves 2-3 follow. TDD per item.

1. **CRIT (TEST-001/QA-001):** shared relay-identifier cleaning on `EgressConfig`
   (`ndi_relay_name`, `sdi_relay_device`): strip padding, 422 on control chars
   and whitespace-only; relay modules reuse the same helper. Per-channel
   try/except isolation in `ChannelAutomationService.run_once` so one
   channel's failure never starves the rest.
2. **ENG-001/TEST-004:** clear state pid before pending-reload `_start`
   (hermetic reload tests fall out); orphan probe returns (name, create_time);
   reap only pre-boot pids not tracked in `_processes`; terminator re-checks
   create_time (closes TOCTOU); direct test of the default psutil pair.
3. **ENG-002:** reload cooldown in `_check_slate_replan` (30s, mirrors #152).
4. **ENG-004:** startup schema-head check -> ERROR log + /health surfacing.
5. **DOC-002 code half + ENG-013:** invalid-device relay = honest `blocked`;
   relay stderr tail captured into status `last_error`.
6. **ENG-003:** boot-time reap of predecessor relay ffmpeg (distinctive
   cmdline `-f decklink`/`-f libndi_newtek`, create-time-guarded, seam-tested).
7-11. Minors: `_stop` discards `_reload_kills`; snapshot-safe relay status
   listing; readiness probe cache + 5s timeout; AccessDenied symmetry;
   `from_env` raises on unknown relay mode (SDI + NDI).

Gate + PR per wave; deploy to the soak box before shutdown.
