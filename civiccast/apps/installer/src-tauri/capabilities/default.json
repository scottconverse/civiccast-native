{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "installer-main",
  "description": "Allows the packaged installer window to use Tauri IPC for local setup actions.",
  "windows": ["*"],
  "permissions": ["core:default", "allow-installer-actions"]
}
