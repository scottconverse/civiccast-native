# CivicCast Records Migrations

Alembic discovers this directory through the root `alembic.ini`
`version_locations` list. Keep signed-record export migrations here and
implement both `upgrade()` and `downgrade()`.
