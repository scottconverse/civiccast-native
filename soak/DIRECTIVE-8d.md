# DIRECTIVE 8d — RUN 2: drive the installer to completion. This is an ACTIVE ORDER.

Your FAIL verdict for card v1 is accepted and stands as the run-1 record. The mission
is NOT over. The installer window from run 1 is still open on this machine, sitting on
the Welcome page. RUN 2 starts now, against the AMENDED card (amendment: "When the
setup window appears, click Next to begin.").

1. USE YOUR COMPUTER-USE CAPABILITY on the open installer window: click Next and drive
   the setup visually through every screen, card-only discipline plus the amendment.
   You are known to be capable of this — do not fall back to side-effect-only
   observation unless a screen is genuinely unreadable, and record any such screen as
   a finding.
2. Log every screen in soak/CARD-RUN-2.md: what it showed, what you did, timestamps,
   MATCHES/DEVIATES/COULD-NOT-PERFORM verdicts, findings numbered continuing from 3.
3. Critical checks along the way: the acquisition screen must show components found
   locally with NO network download of the AI model (this candidate carries that fix —
   watch it happen and record it); at the end, click "Open operator console" per the
   card; verify Start Menu/Desktop shortcuts exist; follow First Setup as far as you
   honestly can; station healthy on :8000.
4. Finish with soak/stranger-verdict-2.json (same schema, run:2). Poll this branch
   every 10 minutes until a directive says the mission is closed — a published verdict
   does NOT end your polling duty.
